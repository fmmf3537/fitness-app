# 健身数据整合分析与指导 APP —— 需求文档

- 版本：V1.0（需求确认稿）
- 日期：2026-08-03
- 用户：个人自用（单用户），架构预留多用户扩展可能
- 状态：经两轮问答确认，待最终确认"待决事项"章节

---

## 1. 项目背景与目标

用户日常同时使用两套系统记录训练：

- **训记（Xunji）**：健身方案生成、训练计划、力量训练的组数/重量/次数记录；
- **佳明 Garmin（Forerunner 955 + Garmin Connect）**：每次运动的心率、时长、热量、睡眠、HRV 等生理数据。

两个系统对同一次训练记录不同维度，数据割裂，无法整体分析。本 APP 的目标是：

1. **融合**：自动识别训记与佳明记录中的"同一次运动"，按规则合并为一份完整训练档案；
2. **分析**：通过 AI 大模型对每次训练点评、对照训记计划给出下一次训练建议；
3. **复盘**：每周、每月自动生成周期性复盘报告；
4. **指导改进**：AI 建议能自动写回训记的部分自动写回（用户确认后），训记计划本身的修改输出建议清单由用户手动调整。

## 2. 需求范围

### 2.1 范围内（In Scope）

| 编号 | 模块 | 说明 |
|---|---|---|
| F1 | 训记数据接入 | 通过训记官方 Open API（用户已申请 Key，已实测连通）读取训练记录与官方计划，支持写回训练记录 |
| F2 | 佳明数据接入 | 通过 `python-garminconnect` 开源库自动拉取活动、心率、睡眠、HRV、Body Battery、训练指标（VO2max 等）全量数据 |
| F3 | 数据融合引擎 | 同一次运动识别 + 字段级合并 + 宽松匹配人工确认队列 |
| F4 | 截图识别兜底 | 接入多模态大模型，在 API 失效/数据缺失时通过截图补录训练数据 |
| F5 | AI 单次点评 | 每次训练后自动生成点评 |
| F6 | AI 下次训练建议 | 对照训记官方计划，给出下一次训练的调整建议 |
| F7 | AI 周期复盘 | 每周、每月复盘报告 |
| F8 | 训记写回 | 用户确认后，将 RPE、组次修正等写回训记（计划为只读，不可写） |
| F9 | 历史数据导入 | 训记与佳明全量历史回溯导入 |
| F10 | 多模型接入 | Kimi、DeepSeek、MiniMax 等国内主流大模型，统一适配层，用户可切换 |
| F11 | 自动化调度 | 每日数据拉取、训练后点评、周/月复盘的定时任务 |
| F12 | 数据看板 | 训练记录、趋势图表、复盘报告的 Web 界面 |
| F13 | 身体数据记录 | 身高/体重/血压/血糖手动录入 + 趋势图表；体重/体脂可与训记身体数据 API 双向同步（2026-08-03 用户更新 Key 后新增能力） |

### 2.2 范围外（Out of Scope）

- ❌ **小米运动健康接入**：官方数据导出流程过于繁琐，用户决定放弃。原"佳明 vs 小米睡眠数据对比"功能随之取消，**睡眠数据以佳明为唯一来源**（见待决事项 D1）。
- ❌ 自动修改训记官方计划（API 只读，技术上不可行）；
- ❌ **饮食/营养记录**：训记饮食 Open API 已随新版 Key 开放（xjfood_ Key，备案于素材/训记key_新版.txt），本期不开发，后续可平滑扩展；
- ❌ 社交功能、多用户。

## 3. 数据源接入方案（已实测验证）

### 3.1 训记 Open API —— 主要通道

> 2026-08-03 已用用户 Key 实测：成功读取当日"背·二头·2"训练，返回标题、起止毫秒时间戳、每动作每组重量/次数/用时、热量备注，与训记分享报表截图完全一致。

| 能力 | 接口 | 说明 |
|---|---|---|
| 读取训练记录 | `POST https://trains.xunjiapp.cn/api_trains_for_llm_v2` | 按 `datestr` 逐日读取；`include_full_data: true` 可获 RPE、组间休息、心率摘要等 |
| 写回训练记录 | `POST /api_upsert_trains_for_llm_v2` | 有 `localid` 更新、无则新建；单次最多 4 条/天、每训练 15 动作、每动作 20 组 |
| 读取官方计划 | `POST https://api.xunjiapp.cn/open/plan/query_gzip` | **只读**；先 `list` 取 `plan_ref`，再 `get` 查日期范围（最长 92 天），gzip 压缩 JSON |

**限频约束**：同日普通读 15s/次、完整读 30s/次、写回 45s/次；`too frequent` 时按 `retry_after_ms` 退避。

**接入要点**：

- 本地缓存：按 `datestr` 缓存，同日不重复请求；写回成功后用服务端返回的标准化数据覆盖缓存；
- 动作名称标准化：以 GitHub `Foveluy/Xunji-movements` 中文标准动作名表为准，写回只传中文 `name`；
- 写回安全规则：**任何写回前必须展示变更摘要（diff），用户确认后才执行**；更新旧训练必须保留 `localid`/`start`/`end` 及 `note` 元数据；
- Key 管理：服务端加密存储，不入日志。

### 3.1b 训记身体数据 API（2026-08-03 新版 Key 新增）

> 新版 `训记key.txt` 追加了身体数据 Skill（独立 Key：`xjbody_...`），接口文档原件存于 `素材/训记key_新版.txt`。

| 能力 | 接口 | 说明 |
|---|---|---|
| 查询身体数据 | `POST https://api.xunjiapp.cn/open/body/query_gzip` | 按日期范围+类型查询；gzip 响应；`records[]` 按日期倒序 |
| 写入身体数据 | `POST /open/body/upsert_gzip` | 按 `datestr+type` upsert；**必须 dry_run 预览 → 用户确认 → `confirmed: true`** |

- 训记原生支持的类型：`weight`(kg)、`bodyfat`(%)、`neck/chest/weist/shoulder/bot` 及各肢体围度(cm)（腰围字段历史拼写为 `weist`，不得"修正"）；**不含身高、血压、血糖**；
- 限频：同 key 同 endpoint 15s/次；
- 本项目策略：体重（及体脂）本地录入后**可选同步至训记**（走确认流）；身高/血压/血糖仅本地存储；
- 同文件还包含饮食数据 Skill（`xjfood_...` Key），本期不开发，仅备案。

### 3.2 佳明 —— 主要通道

- **方式**：`python-garminconnect`（非官方库，模拟 Garmin Connect SSO 登录），服务器端保存登录 token（缓存约一年），每日定时增量拉取；
- **数据范围**：活动详情（含逐秒心率、配速、训练组 exercise sets）、每日健康（步数/静息心率/压力/Body Battery/HRV/血氧）、睡眠分期、体成分、训练指标（VO2max、训练准备度、乳酸阈值等）；
- **风险与对策**：
  - 非官方接口，Garmin 可能变更内部端点导致失效 → 纳入监控告警，失效时降级为"手动从 Garmin Connect 网页导出 FIT/TCX 文件导入"通道（保留文件导入入口作为备份）；
  - 若佳明账号开启 MFA → 首次登录需输入验证码，token 缓存后续期自动；
  - 官方 Health/Activity API 仅面向企业合作伙伴，个人不适用，不采用。
- 官方账号密码/token 加密存储于服务器，仅用于数据拉取。

### 3.3 截图识别 —— 兜底通道

- 场景：训记/佳明接口失效、补录历史缺失数据、或用户随手上传；
- 方式：调用多模态大模型（Kimi 视觉模型等，单张图按 token 计费，单次约几分钱）识别截图，输出结构化 JSON，经用户确认后入库；
- 已验证素材：训记"分享记录"报表版式规整（日期、标题、肌肉热力图、每动作组次表、总消耗/总重量/总耗时），视觉识别可行性高。

## 4. 数据融合引擎（核心）

### 4.1 同一次运动识别规则

主键逻辑：**日期 + 时间区间重叠**。

1. **精确匹配**：训记训练 `[start, end]`（毫秒时间戳）与佳明活动起止时间的重叠率 ≥ 60% → 自动判定同一次运动；
2. **宽松匹配**（应对忘按手表、训记补录）：
   - 同日、时间区间接近（起止差 ≤ 30 分钟）或仅日期相同且类型吻合（佳明"力量训练"↔ 训记力量训练标题）→ 进入**待确认队列**，用户一键确认/拆分为两次运动；
   - 佳明有活动而训记无记录（忘记录）→ 提示"佳明有 X 时 X 分的力量训练，训记无对应记录"，支持一键从佳明数据生成训记草稿（写回需确认）；
   - 训记有记录而佳明无活动（忘按表）→ 标记"无佳明数据"，仅保留训记维度。
3. **一日多练**：同一天有氧、力量分开记录的场景，按时间区间分别匹配，互不干扰（用户确认存在该场景）。

### 4.2 字段级合并规则（用户已确认）

| 数据维度 | 主用源 | 说明 |
|---|---|---|
| 动作名称、组数、每组重量/次数、RPE | **训记** | 动作层面一切以训记为准 |
| 训练时长、总消耗热量、心率（平均/最大/区间/曲线） | **佳明** | 生理与时长数据以手表为准 |
| 训练标题 | 训记 | 佳明活动类型作为标签辅助 |
| 睡眠、HRV、Body Battery、静息心率 | 佳明（唯一来源） | 见 D1 |

冲突处理：两侧同名字段不一致时按上表取主用源，**另一来源数值保留在原始记录中可查看**，不丢弃；融合记录与两侧原始数据建立引用关系，可追溯。

## 5. AI 分析功能

统一通过"多模型适配层"调用，用户可在设置中切换模型（Kimi / DeepSeek / MiniMax 等），各模型 API Key 自行充值、服务端加密存储；界面展示各模型单次调用估算成本与本月累计消耗。

### 5.1 单次训练点评（F5）
- 触发：每日数据拉取后检测到当日有新融合训练 → 自动生成；
- 输入：本次融合训练数据 + 近 4 周同部位/同动作历史 + 近期睡眠/HRV/训练准备度；
- 输出：完成质量点评（重量/容量/密度变化、与历史的 PR 对比）、恢复状态评估、注意事项。

### 5.2 下次训练建议（F6）
- 输入：训记官方计划（只读拉取未来 30 天）+ 本次完成情况 + 恢复指标；
- 输出：对照计划的调整建议清单，明确到**动作 / 重量 / 组数 / 次数**；
- 落地两级：
  - 可写回项（已发生训练的 RPE、难度标记等）→ 展示 diff，确认后调训记写回接口；
  - 计划修改项 → 输出操作指引，用户手动在训记 App 中调整（计划接口只读）。

### 5.3 周期复盘（F7）
- **周复盘**：每周日晚生成——训练频率、部位分布、总容量趋势、PR 记录、睡眠/恢复与训练表现关联、下周重点建议；
- **月复盘**：每月 1 日生成——月度趋势图表、计划完成率、体成分变化（如有佳明体脂数据）、下月计划方向建议；
- 报告以图表+文字呈现，可导出 Markdown/PDF。

## 6. 系统架构

### 6.1 部署形态（用户已确认接受云服务器，低配即可）

- **云服务器**：1 台低配 VPS（建议 2C4G，预估 ¥30–60/月），运行后端服务 + 数据库 + 定时任务；
- **前端 Web 应用**：响应式设计——电脑浏览器直接使用，同时提供 Windows 桌面客户端（Electron/Tauri 套壳，体验等同桌面程序），手机浏览器访问；
- 单用户 + 密码/口令访问，暂不建多用户账号体系。

### 6.2 技术组成

| 层 | 选型建议 | 说明 |
|---|---|---|
| 后端 | Python（FastAPI） | 与 garminconnect 库、数据处理栈同语言 |
| 数据库 | PostgreSQL（或 SQLite 起步） | 融合记录、原始数据、AI 报告、缓存 |
| 任务调度 | APScheduler / cron | 见 §6.3 |
| AI 适配层 | 统一抽象接口 + 各厂商 SDK/HTTP | 模型可插拔、按调用记账 |
| 前端 | React/Vue + 图表库 | 响应式；桌面端 Tauri 套壳 |
| 监控告警 | 服务自检 + 推送（Server酱/邮件） | 佳明接口失效、任务失败时通知 |

### 6.3 自动化任务（用户已确认节奏）

| 任务 | 时间 | 内容 |
|---|---|---|
| 每日数据同步 | 22:47 | 增量拉取当日训记 + 佳明数据 → 融合 → 入待确认队列（如有宽松匹配项） |
| 单次点评 | 数据同步后触发 | 当日有训练则生成点评 + 下次建议 |
| 周复盘 | 每周日 21:13 | 生成周复盘报告并通知 |
| 月复盘 | 每月 1 日 09:23 | 生成月复盘报告并通知 |
| 计划同步 | 每日 | 拉取训记官方计划（未来 30 天）缓存 |
| 接口健康检查 | 每小时 | 佳明/训记/AI 接口可用性，异常推送告警 |

### 6.4 历史数据导入（F9）

- **训记**：按 `datestr` 逐日回溯，遵守 15s 限频（约 5760 天/日上理论限，实际按注册日起逐日扫描空日跳过）；导入任务可断点续传、后台执行；
- **佳明**：首次 `--all` 全量拉取（多年数据量较大，采用限速+缓存），之后每日增量；
- 导入完成后统一跑一遍历史融合，产出全量训练档案。

## 7. 非功能需求

- **安全**：所有第三方 Key/账号 token 加密存储（服务端密钥），传输全 HTTPS；佳明凭据不出服务器；APP 访问需登录口令；
- **隐私**：数据仅存储于自有服务器，不向第三方共享；AI 调用仅发送分析所需的最小数据子集；
- **可靠性**：定时任务失败重试与告警；数据库每日自动备份（保留 30 天）；
- **性能**：单用户量级，常规页面 < 1s；AI 生成类操作异步执行、完成通知；
- **可维护性**：佳明非官方接口封装为独立适配模块，失效时可替换实现而不影响主流程。

## 8. 实施分期建议

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **MVP** | 训记+佳明日同步、融合引擎（含待确认队列）、基础看板 | 连续 7 天数据自动融合，匹配准确率人工抽检 ≥ 90% |
| **V1** | 历史全量导入、AI 单次点评+下次建议（单模型先行）、训记写回（含确认流） | 点评覆盖每次训练；写回 diff 确认流无误写 |
| **V2** | 多模型切换、周/月复盘、截图识别兜底、桌面客户端套壳、监控告警 | 复盘按时生成；截图识别字段准确率 ≥ 95% |

## 9. 待决事项

- **D1**：✅ 已确认（2026-08-03）：放弃小米接入，"睡眠双源对比"功能取消，睡眠/恢复数据以佳明为唯一来源；
- **D2**：AI 模型首批接入顺序建议 Kimi → DeepSeek → MiniMax，其余后续按需扩展——请确认；
- **D3**：✅ 已确认（2026-08-03）：佳明账号未开启两步验证（MFA），服务器首次登录可直接账号密码获取 token；
- **D4**：训记/佳明账号注册时间（历史回溯起点估算用）；
- **D5**：云服务器是否已有（如已有请提供配置），或需要推荐购买方案。
