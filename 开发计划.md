# 开发计划 —— 健身数据整合分析与指导 APP

> 配合《PRD.md》使用。每个任务可直接作为一轮 Kimi Code 的工作指令。
> 建议每完成一个任务就提交一次 Git，保持可回滚。
> 版本 V1.0 · 2026-08-03

## 0. 项目骨架约定

```
fitness-hub/
├── backend/
│   ├── app/
│   │   ├── main.py                # FastAPI 入口
│   │   ├── config.py              # 环境变量与加密
│   │   ├── db.py                  # SQLAlchemy 引擎与 Session
│   │   ├── models.py              # PRD §4 全部表
│   │   ├── adapters/
│   │   │   ├── xunji.py           # 训记 API 封装（读/计划/写回，限频）
│   │   │   ├── garmin_adapter.py  # 佳明封装（登录缓存、拉取、降级导入）
│   │   │   └── llm.py             # 多模型统一适配层 + 记账
│   │   ├── services/
│   │   │   ├── sync.py            # 每日同步编排
│   │   │   ├── matcher.py         # 融合匹配引擎（PRD §5.1）
│   │   │   ├── fuse.py            # 字段级合并（PRD §5.2）
│   │   │   ├── ai.py              # 点评/建议/复盘生成
│   │   │   └── backfill.py        # 历史导入（断点续传）
│   │   ├── api/                   # REST 路由
│   │   └── scheduler.py           # APScheduler 任务注册
│   ├── tests/
│   └── requirements.txt
├── frontend/                      # React + Vite + Tailwind + ECharts
├── docker-compose.yml
└── .env.example                   # XUNJI_API_KEY / XUNJI_BODY_API_KEY / GARMIN_EMAIL / GARMIN_PASSWORD / FERNET_KEY / LLM keys
```

**给 Kimi Code 的上下文约定**：每次开工先把《PRD.md》和本文件喂给它，再描述当次任务。

---

## 阶段一：MVP（目标：本机跑通"同步→融合→看板"闭环）

| # | 任务（可直接作为指令） | 依赖 | 验收 |
|---|---|---|---|
| M1 | 初始化项目骨架：按 §0 建目录，FastAPI 跑通 `/health`，SQLAlchemy 建 PRD §4 全部表（SQLite），`.env` 加载 + Fernet 加密工具 | — | `pytest` 过；表结构与设计一致 |
| M2 | 实现 `adapters/xunji.py`：读训练（含 gzip 处理、限频装饰器、datestr 缓存）、读官方计划（list/get）、写回（先只实现不暴露 UI）；用 2026-08-03 真实数据写集成测试 | M1 | 能拉取并落库 `xunji_train`，重复拉取幂等 |
| M3 | 实现 `adapters/garmin_adapter.py`：garminconnect 登录与 token 缓存、按日期拉活动详情（含 exercise sets）、拉睡眠/HRV/Body Battery 落 `garmin_daily` | M1 | 能拉取近 7 天活动与健康数据并落库 |
| M4 | 实现 `services/matcher.py` + `fuse.py`：严格按 PRD §5.1/§5.2，含待确认队列写入 | M2, M3 | 单元测试覆盖：重叠≥60% 自动配、时间接近入队列、一日两练不串、单边记录 |
| M5 | 实现 `services/sync.py` + `scheduler.py`：每日同步编排（拉取→融合→日志 `job_run`），失败重试 3 次 | M4 | 手动触发 sync 后 workout 表产出正确融合记录 |
| M6 | 前端 M1：Vite+React+Tailwind 骨架，日历视图 + 训练详情页（组次表+心率曲线 ECharts）+ 待确认队列操作（合并/拆分） | M5 | 浏览器可浏览近 7 天融合档案并完成一次人工确认 |
| M7 | 真机联调：连续 7 天日常使用，抽检匹配准确率 ≥ 90%，修 bug | M6 | 达成 PRD US-2 AC6 |

**MVP 完成标志：连续 7 天无需手动干预，看板数据正确。**

## 阶段二：V1（目标：AI 教练上线 + 历史数据齐全）

| # | 任务 | 依赖 | 验收 |
|---|---|---|---|
| V1-1 | `adapters/llm.py`：OpenAI 兼容统一接口，先接 Kimi；设置表存 Key（加密）；`llm_call` 记账 | M1 | 测试调用返回内容且记账正确 |
| V1-2 | `services/backfill.py`：训记逐日回溯（15s 限频、空日跳过、断点表记录进度）；佳明全量拉取（限速+缓存）；完成后跑历史融合 | M5 | 导入进度可查；中断后重跑不重复 |
| V1-3 | `services/ai.py` 单次点评：按 US-6 组装 prompt（本次+近4周同部位+近7天睡眠/HRV），同步任务后自动触发，报告落 `ai_report` | V1-1 | 训练日自动生成点评，内容引用真实数据 |
| V1-4 | `services/ai.py` 下次建议：拉训记计划缓存，输出到动作/重量/组/次粒度；前端按"可写回/需手动"两类展示 | V1-3, M2 | 建议与训记计划对照呈现 |
| V1-5 | 写回确认流：后端 diff 生成 → 前端确认 → 调训记写回 → 服务端返回覆盖缓存；全套留痕 | V1-4 | PRD §5.4 全规则通过测试 |
| V1-6 | 前端 V1：趋势页（4/12 周容量、部位频次）、AI 报告页 | V1-3 | 图表数据与库一致 |
| V1-7 | 身体数据模块：身高/体重/血压/血糖手动录入（date+type upsert）+ 趋势图 + 体重/体脂可选同步训记（dry_run→确认→confirmed 流程）；AI 点评/复盘上下文纳入体重趋势 | M6 | PRD US-12 全 AC；同步确认流无 dry_run 绕过 |

## 阶段三：V2（目标：完整体验）

| # | 任务 | 依赖 | 验收 |
|---|---|---|---|
| V2-1 | LLM 层扩展 DeepSeek、MiniMax，设置页切换默认模型 + 月度成本汇总 | V1-1 | US-9 全 AC |
| V2-2 | 周复盘（周日 21:13）+ 月复盘（每月 1 日 09:23），Markdown/PDF 导出 | V1-3 | US-10 全 AC |
| V2-3 | 截图识别兜底：上传截图 → Kimi 视觉模型 → 结构化 JSON → 预览确认入库 | V2-1 | 训记标准报表字段准确率 ≥ 95% |
| V2-4 | 佳明 FIT/TCX 手动导入降级通道 + 接口健康检查与告警推送 | M3 | 拔 token 模拟失效时告警到达 |
| V2-5 | 部署：Docker Compose（后端+前端+PostgreSQL+备份任务）上云服务器；登录口令 | 全部 | 手机/电脑浏览器均可访问 |
| V2-6 | Tauri 桌面套壳（可选，最后做） | V2-5 | Windows 桌面图标可用 |

## 风险提示（开发时盯紧）

1. **佳明非官方接口**可能失效——`garmin_adapter` 必须独立封装、失败可降级到文件导入，别把它的调用散落到业务代码里；
2. **训记限频**是硬约束——所有训记调用走统一的限频装饰器，历史导入宁可慢不可被封；
3. **写回是高危操作**——V1-5 之前写回接口只允许 `dry_run: true` 测试；
4. **密钥安全**——任何时候 Key 只从环境变量/加密存储读取，Kimi Code 生成代码后检查无硬编码；
5. **匹配准确率**是命门——M7 不达标先调算法（阈值、类型辅助判断），不要带病进入 V1。
