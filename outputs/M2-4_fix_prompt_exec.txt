你之前执行的 M2-4 任务失败：虽然把 backend/app/api/auth.py 里的 require_auth / _ACTIVE_TOKENS / 旧 login 分支删除了，但**14 个业务 API 文件一个都没改成 get_current_user_id**，它们仍在 `from app.api.auth import require_auth` 并 `Depends(require_auth)`，导致整个后端 import 失败、所有 pytest 收集报错（ImportError: cannot import name 'require_auth'）。当前工作区处于半完成态（auth.py / tests/conftest.py 已就绪，services 层部分已改）。

本次任务：把 M2-4 真正补齐完成。必须让 `cd backend && .venv/Scripts/python.exe -m pytest tests/ -q --no-cov` 全量通过（exit 0）。

## 现状（已在仓库中）
- auth.py 已提供 `get_current_user_id(authorization: str|None = Header, session = Depends(get_session)) -> int`。
- tests/conftest.py 已有 `auth_user(client, session)` 和 `auth(auth_user)` 夹具：创建用户 alice/test-pass 并登录，返回 {"user_id":..., "headers": {"Authorization": "Bearer <token>"}}。
- 数据表已有 user_id 列（Workout / XunjiTrain / GarminActivity / GarminDaily / BodyMetric / XunjiPlan / MatchCandidate / AiReport / LlmCall / JobRun / ReportChatMessage / Setting.user_id 等）。

## 必须完成的事（逐一核对，不要遗漏）

### A. 改造 14 个业务 API 文件（核心，之前漏做）
对每个文件：
1. 把 `from app.api.auth import require_auth` 改成 `from app.api.auth import get_current_user_id`。
2. 把路由级 `router = APIRouter(..., dependencies=[Depends(require_auth)])` 的 `dependencies=[Depends(require_auth)]` 去掉（或保留但无害），**并且在每个处理函数签名注入 `current_user_id: int = Depends(get_current_user_id)`**。
3. 所有涉及「用户个人数据」的查询（session.query(Model) / select(Model)）追加 `.filter(Model.user_id == current_user_id)`；
4. 所有创建数据的操作写入 `user_id=current_user_id`（若走 services 层函数，把 current_user_id 透传进去）。

涉及文件（均已确认仍 import require_auth，必须改）：
- backend/app/api/ai_reports.py
- backend/app/api/backfill.py
- backend/app/api/body_metrics.py
- backend/app/api/fit_import.py
- backend/app/api/llm.py
- backend/app/api/match_candidates.py
- backend/app/api/plans.py
- backend/app/api/posters.py
- backend/app/api/screenshot.py
- backend/app/api/settings.py
- backend/app/api/stats.py
- backend/app/api/sync.py
- backend/app/api/workouts.py
- backend/app/api/writeback.py

### B. 数据隔离细节（参考）
- workouts.py：calendar/deleted/workout_detail 的 Workout 查询加 user_id 过滤；delete/restore 前确认 workout.user_id == current_user_id，否则 404。
- settings.py：GET/PUT 按 `Setting.user_id == current_user_id` 取自己那行（每用户一行），不要用全局第一行。
- sync/backfill/fit_import 等批量写入：把 current_user_id 透传到 services 层写入函数，每行带 user_id。services/workouts.py 的 delete_workout/restore_workout/list_deleted_workouts 增加 user_id 参数按 user_id 过滤（已在 partial 改动基础上补齐）。
- LLMCall、JobRun、AuditLog 等系统级表不加 user_id 过滤。

### C. 修复现有测试（关键）
现有 122+ 个 test_api_*.py 依赖旧 require_auth 的 client 夹具。由于 require_auth 已删除，必须改用新认证：
- 在每个 test_api_*.py 里，用 conftest 的 `auth` 夹具（返回 Authorization 头）作为 client 请求头；若测试需要具体 user_id 来构造数据，用 `auth_user` 夹具的 user_id。
- 构造业务数据（workout/body_metric/setting 等）时传入 user_id=auth_user["user_id"]，否则 API 按 current_user_id 过滤后该数据不可见，测试会 404。
- 若某 test_api_*.py 自己定义了 client 夹具且 override 了 get_session，确保它也能配合 auth_user（auth_user 已依赖 client + session）。
- 目标：所有 test_api_*.py 收集通过且用例通过。

### D. test_api_isolation.py 已存在（可能半成品）
检查 backend/tests/test_api_isolation.py：若它引用了未定义的夹具或旧命名，修正它；它应验证「用户 A 数据用户 B 不可见（404/401）」「无 token 访问 401」「带 token 访问自己数据 200」。

### E. 验证（必须跑，且全绿）
- `cd backend && .venv/Scripts/python.exe -m pytest tests/ -q --no-cov`
- 必须 0 collection error，所有用例 passed（exit 0）。
- 额外跑 `pytest tests/test_auth.py tests/test_api_isolation.py -q --no-cov` 确认认证与隔离用例通过。

### F. 要求
- 仅修改上述 api/*.py、services 层必要的函数签名、tests 下的测试文件。
- **不要**再删除 require_auth（已删）；**必须**确认 `grep -rn "require_auth" backend/app backend/tests` 除注释/字符串外无 import 残留。
- 不自行 commit，仅留工作区待审核。

### 验收标准（硬指标）
1. `grep -rn "require_auth" backend/app` 无残留 import（仅注释/文档字符串可保留）。
2. `pytest tests/ -q --no-cov` 全量 passed，exit 0，0 collection error。
3. 任意业务查询按 current_user_id 过滤，跨用户访问 404/401。
4. 新建数据带 user_id。
