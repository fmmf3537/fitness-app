# M2-2 审计结论：用户模型 CRUD 服务

**提交状态**：Kimi 已生成工作区文件，尚未 commit（待本轮审核后由 WorkBuddy 统一提交）。
**分支**：`multiuser-v2`（main 未动）。
**审计日期**：2026-08-24

## 一、交付物
- 新建 `backend/app/services/users.py`：模块级函数式 CRUD 服务（遵循 `session = session or SessionLocal()` 惯例）。
- 新建 `backend/tests/test_users_service.py`：21 个 pytest 用例。

## 二、代码核查（逐条对照提示词验收标准）

| 验收项 | 核查结果 | 证据 |
|---|---|---|
| 密码全程 bcrypt，无明文 | ✅ | `create_user`/`update_user` 均调 `hash_password`；测试断言 `password_hash.startswith("$2")` 且 ≠ 明文 |
| 用户名唯一、非空校验 | ✅ | `get_user_by_username` 查重；空/空白抛「用户名不能为空」；短密码抛「密码长度不能少于 6 位」 |
| 软停用而非硬删除 | ✅ | `deactivate_user` 仅置 `is_active=False`，`test_users_are_never_hard_deleted` 验证行数不变 |
| 审计日志动作齐全 | ✅ | `create_user`/`update_user`/`deactivate_user` 均写 AuditLog；测试逐条断言 action/actor/target/summary |
| 测试覆盖且全部通过 | ✅ | **实测 `.venv/Scripts/python.exe -m pytest tests/test_users_service.py -q --no-cov` → 21 passed**（非依赖自述） |
| 不改动 models/auth/api | ✅ | git status 显示仅新增上述两文件，未触碰 `auth.py`、`models.py`、`api/*.py` |

## 三、正向评价
- 审计写入与业务写操作共用同一事务（commit 前 add AuditLog），保证一致性，无独立 commit race。
- 异常时 `rollback()` 处理完整；`verify_user_password` 对不存在/停用/错误密码统一返回 None，不抛异常，符合安全惯例（避免账号枚举）。
- 测试覆盖了「跨用户共存」之外的软删除边界，质量高于提示词最低要求。

## 四、P1/P0 风险与待确认项
- **P2（安全）**：无 P0/P1 风险。
- **边界确认**：当前 `users.py` 未做「admin 不能被停用」等业务约束——按提示词「不擅自实现 PRD 未覆盖场景」原则，本次不做，已符合规则；若后续需要，建议记入 `docs/TECH_DEBT.md`。
- **遗留 _tmp 文件**：`backend/` 下仍有 9+ 个 `_tmp_*` 文件（含 `_tmp_check_db.py`、`_tmp_pytest.log`、空壳 `backend/backend/` 错位库等）。这些为 Kimi 执行期产物，按你先前「保留不动」决策，未处理。后续 M2 收尾时再决定是否清理。
- **oxlint**：Kimi 报告 oxlint 通过（`.bin` shim 缺失，用 `node node_modules/oxlint/bin/oxlint` 跑通），不影响交付。

## 五、结论
M2-2 实现正确、测试充分、边界清晰，**审计通过**。建议提交，并继续推进 M2-3。

## 六、下一步
- 提交 M2-2（连同本审计报告）。
- M2-3：登录 API 改造 + auth_token 表（见 `M2-3_提示词_2026-08-24.md`），由 Kimi 执行、WorkBuddy 审计。
