# M2-3 审计结论：登录 API 改造 + auth_token 表

**提交状态**：Kimi 已生成工作区文件，尚未 commit（待本轮审核后由 WorkBuddy 统一提交）。
**分支**：`multiuser-v2`（main 未动）。
**审计日期**：2026-08-24

## 一、交付物
- `backend/app/models.py`：新增 `AuthToken`（`auth_token` 表，`user_id` FK→users.id ON DELETE CASCADE + index、`token` String(64) unique+index、`created_at`、`expires_at`、`is_active`）。
- `backend/alembic/versions/55163cf925d2_add_auth_token.py`：仅建/删 auth_token，down_revision=`a0593c2c6cf2`（M1-3 head），链线性无分叉。
- `backend/app/api/auth.py`：登录/登出/`get_current_user_id` 依赖。
- `backend/tests/test_auth.py`：13 个用例。

## 二、代码核查（实测，非依赖自述）

| 验收项 | 结果 | 证据 |
|---|---|---|
| 登录 username+password，token 落库且 7 天过期 | ✅ | `_get_active_token` 查 is_active & expires_at>now；`expires_at=utcnow()+timedelta(days=7)` |
| 错误密码/不存在/已停用 → 401，零落库 | ✅ | test_auth 覆盖；统一提示「用户名或密码错误」防枚举 |
| `get_current_user_id` 返回 user_id 或 401 | ✅ | 有效 token→user_id；缺失/格式错/不存在/过期/失效→401 |
| 登出软失效 token | ✅ | logout 置 is_active=False，不影响其他 token |
| 迁移跑通且只建 auth_token | ✅ | **实测**：`alembic upgrade head` 仅新增 `auth_token`（17 表→18 表），未误改其他表 |
| 测试全过 | ✅ | **实测 `.venv/Scripts/python.exe -m pytest tests/test_auth.py tests/test_users_service.py --no-cov` → 35 passed**；业务 API 回归 122 passed |
| 不破坏现有 services/users.py、业务 api | ✅ | git 仅改 models.py、auth.py、新增迁移与测试；业务 api/*.py 未动 |

## 三、兼容性说明（记录在 TECH_DEBT，需 M2-4 闭环）
- `LoginRequest.username` 当前为**可选**：username 缺省时保留旧「APP_PASSWORD + 内存 token」分支，且 `require_auth` 原样保留 —— 目的是不打断现有 14 个业务 api 与 ~20 个旧测试（它们仍依赖旧路径）。
- Kimi 已在 `docs/TECH_DEBT.md` 记录 M2-3-a/b/c：M2-4 必须把业务 API 切换到 `get_current_user_id`，并**删除**兼容分支与 `require_auth` / `_ACTIVE_TOKENS`。

## 四、风险与建议
- **P2（安全，可接受）**：M2-3 单独看存在「两套认证并存」的临时态，但按阶段拆分是既定计划，**M2-4 必须闭环删除旧路径**，否则会遗留双认证安全隐患。务必在 M2-4 收尾时确认删除。
- **P2（安全）**：无 P0/P1。

## 五、结论
M2-3 实现正确、测试充分、迁移干净，**审计通过**。建议提交，并继续推进 M2-4（含兼容分支清理）。

## 六、下一步
- 提交 M2-3（连同本审计报告、M2-4 提示词）。
- M2-4：全 API 权限注入 + 删除旧兼容分支（见 `M2-4_提示词_2026-08-24.md`）。
