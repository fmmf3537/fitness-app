你正在参与一个健身数据整合分析 APP 的多用户化改造项目（WorkBuddy 协作开发）。
通用铁律（必须遵守）：
1. 只修改任务中明确列出的文件，不要动其他文件。
2. 密码必须使用 bcrypt 哈希，cost=12；禁止明文或可逆加密存储。
3. 新增代码必须有对应测试用例。
4. 完成后运行 pytest 和 oxlint，确保 0 error。
5. 发现 PRD 未覆盖的场景，不要擅自实现，记录在 docs/TECH_DEBT.md。

当前任务：创建密码哈希工具

新建文件：backend/app/utils/__init__.py（空文件，确保包可被导入）

新建文件：backend/app/utils/password.py
实现两个函数：
1. hash_password(plain: str) -> str
   - 使用 bcrypt（优先 bcrypt 库；若项目未安装，使用 pip install bcrypt 安装到 backend 的虚拟环境 backend/.venv）。
   - 使用 bcrypt 哈希，cost（log rounds）= 12。
   - 返回字符串格式哈希（bcrypt 默认返回 bytes，需 decode 为 str）。
   - 必须处理密码长度上限（bcrypt 限制  ̀ 72 字节）：若 plain 超过 72 字节，应先截断或抛 ValueError（明确提示）。
2. verify_password(plain: str, hashed: str) -> bool
   - 验证明文密码与哈希是否匹配，返回 bool。
   - 处理 hashed 为 None 或非法格式的情况：返回 False（不抛异常）。

新建文件：backend/tests/test_password.py
测试用例必须覆盖：
- hash_password 对已知明文产生非空、以 $2 开头的 bcrypt 哈希字符串；
- verify_password 正确密码返回 True、错误密码返回 False；
- 相同明文多次 hash_password 结果不同（salt 随机）；
- 验证 verify_password(None, ...) 与 verify_password(..., None) 不抛异常、返回 False。

要求：
- 仅新建上述三个文件，不修改 models.py、auth.py、任何其他文件。
- 运行 cd backend && .venv/Scripts/python.exe -m pytest tests/test_password.py -q 通过。
- 确认 backend/.venv 已安装 bc rypt；未安装则用 backend/.venv/Scripts/pip.exe install bcrypt 安装后重试。

完成后：不要自行 commit。仅把改动留在工作区，由审核方审查后提交。