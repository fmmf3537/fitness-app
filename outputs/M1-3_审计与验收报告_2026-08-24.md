# M1-3 审计与验收报告（2026-08-24）

## 任务范围
按 `outputs/开发提示词手册_多用户版_V1.0.md` 的 M1-3 定义，对 11 张业务表加 `user_id`（FK→users），并将原有的单列/双列 UNIQUE 改为「(user_id, 自然键)」复合唯一，生成 Alembic 迁移。

## 交付内容（已提交 `multiuser-v2` @ `b4d037e`）
- `backend/app/models.py`：
  - `xunji_train` / `garmin_activity` / `garmin_daily` / `body_metric` / `xunji_plan`：UNIQUE 改为复合唯一（含 `user_id`），列名 `uq_<表>_user_<自然键>`。
  - `workout` / `match_candidate` / `ai_report` / `llm_call` / `job_run` / `report_chat_message`：仅加 `user_id` 列（FK users, nullable）。
  - 所有 `user_id` 统一 `nullable=True`（含 `xunji_train`），与 M1-2 一致，平滑升级；M1-4 回填存量后再收紧。
- `backend/alembic/versions/a0593c2c6cf2_add_user_id_to_business_tables_m1_3.py`：
  - batch 模式兼容 SQLite，upgrade/downgrade 对称可逆。
  - `down_revision = a7b8c9d0e1f2`，链路线性无分叉。
- `backend/tests/test_models.py` + `test_alembic.py`（由我补的测试修正，详见「问题与修复」）。

## 测试结果（硬证据）
- `pytest tests/test_models.py tests/test_alembic.py` → **27 passed**（20 models + 7 alembic）。
- 关键验证点：
  - 复合 UNIQUE 语义：同一 user_id 重复自然键 → 冲突；不同 user_id 相同自然键 → 共存。
  - 迁移升级/回滚往返：settings 改名保留数据、report_chat_message 列集合、downgrade 到 M1-2 前 head 均通过。

## 问题与修复（Kimi 未覆盖，由我补）
Kimi 的改动本身正确，但遗留了**断言与新 schema 不匹配**的测试回归，不满足「提交即绿」。我在审计中修复：

1. **4 个 UNIQUE 测试断言失效**（原断言「两行 NULL 冲突」语义已被复合约束改变）：改写为「同用户冲突 / 跨用户共存」双验证；初版误在同一用户身上叠加，已修正为使用独立 u3 用户，避免自冲突。
2. **`test_alembic.py::test_settings_per_user_migration_roundtrip`**：`downgrade("-1")` 在 M1-3 加入新 head 后只能后退一层，硬编码为 `downgrade("1f0d04e38eb5")`（M1-2 前 head）。
3. **`test_alembic.py::test_report_chat_message_table_roundtrip`**：M1-3 给该表新增 `user_id`，精确集合断言需补 `user_id`。

## Kimi 执行副作用（待清理）
Kimi 在跑迁移测试时于 `backend/` 下生成了 9 个临时文件（调试副产物，未被项目代码引用），详见下方「待清理清单」。它们不影响功能，但污染仓库，建议删除。

## 结论
✅ M1-3 功能与迁移正确、测试全绿，已提交 `multiuser-v2`，`main` 未动。**多用户数据隔离的「字段层」已就绪，但查询作用域过滤仍未实现（M-M2），当前业务代码尚未按 user_id 隔离数据。**

## 待清理清单（需你确认）
以下 9 个文件均为 Kimi 生成的临时产物，位于项目目录 `backend/`，未被任何代码引用：
- `backend/_tmp_check_db.py`（3.7 KB）
- `backend/_tmp_cleanup_db.py`（0.9 KB）
- `backend/_tmp_h/`（空目录）
- `backend/_tmp_handle.exe`（761 KB，可执行，来路不明）
- `backend/_tmp_handle_out.txt`（71 B）
- `backend/_tmp_lsof.sh`（0.5 KB）
- `backend/_tmp_rt.db`（~486 MB，临时 SQLite）
- `backend/_tmp_test.db`（~261 MB，临时 SQLite）
- `backend/_tmp_who_locks.ps1`（2.6 KB）

合计约 750 MB。删除前我会先备份到 `$HOME/.workbuddy/backup_m1-3_tmp/` 并确认成功，再走 Windows 回收站机制（无 trash CLI，需二次确认）。**在你明确确认前，我不会删除任何文件。**

## 下一步
- 确认清理临时文件后，进入 **M1-4**（存量数据迁移脚本：建默认管理员、回填各表 `user_id`）。
- M1-4 完成后，多用户数据层（M-M1）才真正闭环，再进入 M-M2（认证/授权）让隔离生效。
