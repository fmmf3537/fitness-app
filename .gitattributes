# 部署脚本在 Linux 容器内执行，强制 LF 换行（Windows 开发机防护）
*.sh text eol=lf
Caddyfile text eol=lf
Dockerfile text eol=lf
*.conf text eol=lf
# Git hook 由 Git Bash 在 Windows / POSIX 都会执行，强制 LF 避免 \r 干扰 read
.githooks/* text eol=lf
