# Kimi Code 开发提示词手册 —— 健身数据整合分析与指导 APP

> 版本 V1.0 · 2026-08-03
> 配套文档：《PRD.md》《开发计划.md》（必须与本文件放在同一项目目录）
>
> **使用方法**
> 1. 在 Kimi Code 中打开项目根目录（内含本文件 + PRD.md + 开发计划.md）；
> 2. 每个新会话先粘贴【P0 全局上下文】，再粘贴当次任务的提示词；
> 3. 严格按 Sprint 顺序执行，一个任务一个会话（或上下文明显变长时开新会话重贴 P0）；
> 4. 每个 Sprint 结束后执行该 Sprint 的【评审与复盘】提示词；
> 5. 任何提示词执行后如测试未全绿，不要让 AI 跳过——修复后再进入下一任务。

---

## P0 全局上下文（每个新会话先贴这段）

```
你是本项目的专职全栈工程师。项目根目录下有《PRD.md》《开发计划.md》《开发提示词手册.md》三份文档，
请先完整阅读 PRD.md 和开发计划.md，之后所有开发严格以它们为准。

【铁律，任何任务都适用】
1. 技术栈以 PRD §2 为准，不得擅自更换框架或引入未列明的重型依赖；
2. 数据模型以 PRD §4 的 SQL 为准；匹配算法以 PRD §5.1 伪代码为准；训记接口格式以 PRD §6.1 为准；
3. 所有密钥/token 只从环境变量或加密存储读取，代码中禁止出现任何硬编码密钥；
   训记训练 Key 读 XUNJI_API_KEY，训记身体数据 Key 读 XUNJI_BODY_API_KEY，
   佳明凭据读 GARMIN_EMAIL / GARMIN_PASSWORD；
4. 训记 API 的所有调用必须经过统一的限频装饰器（读 15s / 完整读 30s / 写回 45s）；
5. 训记写回接口在 V1-5 任务之前，所有代码路径强制 dry_run: true；
6. 佳明接入只允许通过 adapters/garmin_adapter.py 暴露的函数调用，业务代码不得直接 import garminconnect；
7. 严格 TDD：先写会失败的自动化测试 → 实现 → 测试全绿 → 重构；禁止"先实现后补测试"；
8. 后端用 pytest（含 pytest-asyncio、pytest-cov），前端用 Vitest + Testing Library；
   每个任务完成时新增代码行覆盖率 ≥ 85%，整体覆盖率不低于 80%；
9. 每个任务完成后：跑全部测试确认无回归 → git add + commit（commit message 格式：
   "[任务号] 简述"，如 "[M2] 训记适配器与限频"）→ 输出一段不超过 10 行的中文完成报告
   （做了什么/测试结果/覆盖率/遗留问题）；
10. 遇到需求模糊处，先列出你的假设并继续按最合理假设实现，不要停下来反复追问；
    假设记录在 commit message 之后。

如果理解以上规则，回复"已阅读并理解"，然后等待任务指令。
```

---

## Sprint 1：数据接入地基（任务 M1–M3）

**Sprint 目标**：项目骨架可运行，训记与佳明两个适配器能用真实凭据拉取数据并幂等落库。

### M1 项目骨架与数据库

```
任务 M1：初始化项目骨架与数据库。

按开发计划 §0 的目录结构创建 backend/ 与 frontend/ 骨架（frontend 本任务只建空壳 vite 工程即可，不做页面）。
后端要求：
1. FastAPI 应用，/health 返回 {"status": "ok"}；
2. SQLAlchemy 2.x + SQLite（数据库文件 backend/data/app.db），按 PRD §4 建全部 10 张表，
   用 Alembic 管理迁移（初始迁移即全部表）；
3. config.py：从 .env 读取全部配置（提供 .env.example，列出 XUNJI_API_KEY、GARMIN_EMAIL、
   GARMIN_PASSWORD、FERNET_KEY、KIMI_API_KEY、DEEPSEEK_API_KEY、MINIMAX_API_KEY、DATABASE_URL）；
   实现 Fernet 加密/解密工具函数 encrypt_value / decrypt_value，主密钥从 FERNET_KEY 读取；
4. models.py 与 PRD §4 字段一一对应，含全部 UNIQUE 约束与外键。

TDD 要求（先写测试再实现）：
- /health 返回 200 的 API 测试；
- 每张表插入+查询的模型测试（至少覆盖全部 UNIQUE 约束触发 IntegrityError 的用例）；
- 加密工具往返测试（encrypt→decrypt 还原原文）；
- Alembic upgrade/downgrade 往返测试。

完成后跑 pytest 全绿 + 覆盖率报告，按 P0 第 9 条提交并输出完成报告。
```

### M2 训记适配器

```
任务 M2：实现 adapters/xunji.py 训记 API 适配器。

接口格式严格按 PRD §6.1。要求：
1. XunjiClient 类，方法：
   - fetch_trains(datestr, include_full_data=False) -> 落库 xunji_train（按 (datestr, localid) upsert，幂等）
   - fetch_plan_list() / fetch_plan(plan_ref, start_date, end_date) -> 落库 xunji_plan
   - upsert_trains(payload, dry_run=True) -> 本任务只实现方法本体，所有调用强制 dry_run=True
2. 响应自动处理 gzip（Content-Encoding: gzip 时先解压再 json）；
3. 统一限频装饰器：普通读 15s、完整读 30s、写回 45s（按"同一 datestr"维度计时）；
   收到 too frequent 时按返回的 retry_after_ms 等待后重试，最多 3 次；
4. 按 datestr 的读取缓存：同日已拉过且数据未过期（同一天内）则直接读库不发请求；
5. 原始响应完整存入 raw_json 字段。

TDD 要求：
- 用 respx 或 responses 库 mock HTTP，禁止测试真实外呼；
- 测试用例至少含：正常读取落库、gzip 响应解析、幂等重复拉取不产生重复行、
  限频计时触发等待、too frequent 重试、缓存命中不发请求、写回强制 dry_run；
- 另写一个标记为 @pytest.mark.integration 的手动测试（默认跳过），
  用真实 XUNJI_API_KEY 拉 2026-08-03 的数据验证连通性（该日有真实训练"背·二头·2"）。

完成后按 P0 第 9 条提交并输出完成报告。
```

### M3 佳明适配器

```
任务 M3：实现 adapters/garmin_adapter.py 佳明数据适配器。

要求：
1. GarminClient 类，封装 garminconnect 库：
   - login()：token 缓存于 ~/.garminconnect；过期时自动用 GARMIN_EMAIL/GARMIN_PASSWORD 重登；
   - sync_activities(date) -> 拉取该日全部活动（含 exercise sets 详情）落库 garmin_activity，
     按 activity_id upsert 幂等；
   - sync_daily(date) -> 拉取睡眠/HRV/Body Battery/静息心率/压力落库 garmin_daily，按 date upsert；
2. 所有网络异常包装为 GarminAdapterError 抛出，调用方不接触 garminconnect 的原始异常；
3. 全局限速：任意两次佳明 API 调用间隔 ≥ 0.5s，防止触发风控；
4. 预留 import_fit_file(path) 方法签名与空实现（V2-4 再实现），写 TODO 注释。

TDD 要求：
- mock garminconnect 库的全部外部调用；
- 测试用例至少含：活动落库幂等、每日健康 upsert、token 过期自动重登逻辑、
  限速间隔生效、异常包装为 GarminAdapterError；
- 另写 @pytest.mark.integration 手动测试（默认跳过）：真实凭据拉近 3 天数据。

完成后按 P0 第 9 条提交并输出完成报告。
```

### Sprint 1 评审与复盘

```
Sprint 1 评审与复盘。

1. 跑全部测试（pytest + 前端已有测试），输出覆盖率报告；
2. 我手动跑过两个 integration 测试后告诉你结果，你根据结果修复问题（如果我现在说"全部通过"则跳过修复）；
3. 检查是否满足 Sprint 目标：M1/M2/M3 验收标准逐条核对，列出"达成/未达成"；
4. 复盘：列出本 Sprint 中实现与 PRD 有偏差的地方、技术债清单（写入 docs/TECH_DEBT.md）；
5. 全部通过后打 git tag "sprint-1"。
```

---

## Sprint 2：融合引擎与看板（任务 M4–M7）

**Sprint 目标**：训记+佳明数据自动融合，浏览器可查看训练档案并处理待确认队列；真机 7 天验证匹配准确率 ≥90%。

### M4 匹配与融合引擎

```
任务 M4：实现 services/matcher.py 与 services/fuse.py。

匹配算法严格按 PRD §5.1 伪代码实现：
- 第一轮：时间重叠率（重叠时长 ÷ 较短区间时长）≥ 0.6 自动匹配，match_status='auto_matched'；
- 第二轮：起止差 ≤ 30 分钟 → 写 match_candidate（status='pending'），两侧记录从待配池移除；
- 剩余：训记单边 match_status='xunji_only'；佳明单边 match_status='garmin_only'，
  且佳明活动类型为力量训练（strength_training 等）时在 match_candidate 写 reason='garmin_only_strength'
  提示可生成训记草稿；
- 同一天多次训练必须各自独立匹配。

融合规则严格按 PRD §5.2：movements_json 取训记，duration_s/calories/avg_hr/max_hr 取佳明，
标题取训记，佳明活动类型存为标签字段。融合产出写 workout 表，保留两侧外键。

TDD 要求（本任务是命门，测试必须充分）：
- 构造夹具数据覆盖：完全重叠、重叠 59%（不自动配）、重叠 61%（自动配）、
  起止差 29/31 分钟边界、一日两练（早有氧晚力量各自配对）、单边记录两种、
  佳明力量单边入待确认、多次运行幂等（不重复建 workout）；
- fuse 测试：字段来源优先级、缺佳明数据时 workout 仅训记维度、外键引用正确；
- 覆盖率本模块要求 ≥ 90%。

完成后按 P0 第 9 条提交并输出完成报告。
```

### M5 同步编排与定时任务

```
任务 M5：实现 services/sync.py 与 scheduler.py。

1. daily_sync(date) 编排：拉训记 → 拉佳明活动+健康 → 跑 matcher+fuse → 写 job_run 日志；
   任一步失败重试 3 次（指数退避 1s/4s/16s），最终失败 job_run 记 status='failed' 与 error；
2. APScheduler 注册（scheduler.py）：
   - 每日 22:47 daily_sync（当日）
   - 每日 22:52 补一次 daily_sync（前一日，防跨零点训练漏拉）
   - 每小时接口健康检查（训记/佳明各一次轻量调用，失败写 job_run，V2-4 再接推送）
   - 每日 23:03 拉取训记官方计划（未来 30 天）缓存
3. 提供 API：POST /api/sync/{date} 手动触发同步（前端调试与补录用）。

TDD 要求：
- mock 两个适配器，测试编排顺序、失败重试次数与退避、job_run 记录正确性、
  重复执行幂等；
- 调度器测试用 APScheduler 的 MemoryJobStore 验证任务注册与触发时间配置。

完成后按 P0 第 9 条提交并输出完成报告。
```

### M6 前端看板

```
任务 M6：实现前端 MVP（frontend/，React + Vite + Tailwind + ECharts）。

页面：
1. 日历视图（默认页）：月历标记有训练的日子（颜色区分 auto_matched/xunji_only/garmin_only/pending），
   点击进当日详情；
2. 训练详情页：融合训练卡片——动作×组次表（重量×次数×组，含 RPE）、
   心率曲线（ECharts 折线，数据来自佳明原始 raw_json 中的心率序列）、
   时长/热量/平均/最大心率摘要，可切换查看"融合/训记原始/佳明原始"三个标签；
3. 待确认队列页：列出 match_candidate（pending），每条提供「合并」「保持分开」两个按钮，
   调用后端接口更新状态并触发/撤销融合；
4. 顶部导航 + 简单登录口令页（调用后端会话接口，口令错误拒绝进入）。

后端需要补的 API：GET /api/workouts/calendar?month=、GET /api/workouts/{id}、
GET /api/match-candidates、POST /api/match-candidates/{id}/resolve、POST /api/auth/login。

TDD 要求：
- 后端新 API 全部有 pytest 用例（含权限拒绝用例）；
- 前端组件用 Vitest + Testing Library：日历渲染、详情页三标签切换、
  待确认操作调用正确接口（mock fetch）；
- 前端构建通过（npm run build）。

完成后按 P0 第 9 条提交并输出完成报告。
```

### M7 真机联调周

```
任务 M7：真机联调与匹配准确率验证。

这是一个验证型任务，不写新功能：
1. 我已在真实环境连续使用 7 天。请帮我写一个分析脚本 scripts/match_audit.py：
   统计近 7 天 workout 表中各 match_status 分布、列出全部人工纠正过的匹配
   （match_candidate 中被人工改判的）、计算自动匹配准确率（自动匹配且未被人工纠正 ÷ 自动匹配总数）；
2. 运行脚本输出报告；
3. 若准确率 < 90%：分析误配样本，提出阈值或规则修正方案（例如按活动类型辅助判断、
   调整重叠率阈值），经我确认后修改 matcher.py 并补充对应回归测试；
4. 将 7 天真实数据固化为 tests/fixtures/ 下的匿名化夹具，作为融合引擎的长期回归测试集。

完成后输出准确率结论（达标/未达标+修正措施），按 P0 第 9 条提交。
```

### Sprint 2 评审与复盘

```
Sprint 2 评审与复盘。

1. 全部测试跑绿 + 覆盖率报告（融合引擎 ≥90%）；
2. 逐条核对 PRD 中 US-1/US-2/US-3/US-4 的全部验收标准，列出达成情况；
3. MVP 完成标志核对：连续 7 天无需手动干预、看板数据正确；
4. 更新 docs/TECH_DEBT.md，打 git tag "sprint-2"（即 MVP 里程碑）。
```

---

## Sprint 3：AI 点评与历史导入（任务 V1-1～V1-3）

**Sprint 目标**：LLM 适配层上线，训练日自动生成单次点评；历史数据全量导入完成。

### V1-1 LLM 适配层（2026-08-06 修订：首发 DeepSeek + MiniMax，Kimi 延后）

```
任务 V1-1：实现 adapters/llm.py 多模型统一适配层（首发 DeepSeek + MiniMax，Kimi 留桩）。
注意：PRD §6.3 已于 2026-08-06 更新，接入顺序以 PRD 为准，不以旧手册为准。

1. 统一接口：chat(messages: list[dict], model_override=None, **opts) ->
   {content, prompt_tokens, completion_tokens}，OpenAI 兼容协议；
2. Provider 注册表按 PRD §6.3 三行：DeepSeek（deepseek-chat，首发默认）、
   MiniMax（MiniMax-M2）、Kimi（留 NotImplementedError 桩）；
   MiniMax-M2 输出含 <think>...</think> 块，content 落库前必须剥离（保留正文）；
3. Key 从 settings 表读取（Fernet 加密存储），提供设置 API：GET/PUT /api/settings/llm
   （PUT 时先调一次该厂商轻量接口验证 Key 有效再保存；无效 Key 拒绝入库）；
4. 每次调用写 llm_call 记账（provider/model/purpose/token 数/成本估算/状态）；
   prompt_tokens/completion_tokens 必须取自 API 响应的 usage 字段（禁止本地估算），
   成本单价做成可配置的常量表；
5. 调用失败重试 2 次，仍失败抛 LLMError；
6. .env 中 DEEPSEEK_API_KEY / MINIMAX_API_KEY 已配置且实测可用。

TDD 要求：mock HTTP；测试统一接口返回结构、<think> 剥离（含无闭合标签的异常输入）、
记账字段取自 usage、无效 Key 拒绝保存、重试逻辑、成本计算；
integration 手动测试（RUN_LLM_INTEGRATION=1 显式门禁，与其他适配器一致）：
真实调 DeepSeek 发"你好"并断言 usage 字段非零。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V1-2 历史数据导入

```
任务 V1-2：实现 services/backfill.py 历史全量导入。

1. 训记：从注册日（配置项 BACKFILL_START_DATE，已确认为 2023-02-01）逐日回溯至今：
   - 严格遵守 15s 限频；返回空训练的日期记录到 backfill_progress 表（新建，字段：
     source/date/status/finished_at），跳过不重拉；
   - 可断点续传：重启后从最后一个未完成日期继续；
2. 佳明分两类（PRD US-5 已确认策略）：
   - 活动列表：2017 年起全量，分页拉取（每页 100 条，间隔 0.5s+）；
   - 每日健康（睡眠/HRV/BB 等）：仅从 BACKFILL_START_DATE 起，与训记对齐；
   - 全程单会话长拉取（禁止重复登录触发 IP 级 429），429 指数退避；
   - 同样记录进度可断点；
3. 全部完成后自动跑一遍历史 matcher+fuse；
4. API：GET /api/backfill/status 返回进度百分比与预计剩余时间，POST /api/backfill/start。
   导入全程后台线程执行，不阻塞服务。

TDD 要求：mock 适配器构造 3 年稀疏数据；测试断点续传、空日跳过、限频计时、
完成后自动触发融合、进度接口返回正确。

开发完成并提交后，执行真实启动：
1. 从项目根目录启动后端（SCHEDULER_ENABLED=true，PYTHONPATH 指向 backend，加载真实 .env），后台常驻；
2. 调 POST /api/backfill/start 启动真实历史导入；
3. 等 2 分钟后调 GET /api/backfill/status，把返回的进度 JSON 附在完成报告里；
4. 不要kill后端进程——导入要跑数小时，让它继续跑，并在报告中写明服务端口和预计完成时间。backfill 主循环必须按天容错：单日失败（网络错误/限频）记录 backfill_progress 后继续或暂停重试，不得因单日异常终止整个导入线程；补对应单测。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V1-3 AI 单次点评

```
任务 V1-3：实现 services/ai.py 的单次训练点评（type='session_review'）。

1. 触发：daily_sync 完成后，若当日存在融合 workout 则异步生成；
2. Prompt 组装（严格按 US-6）：
   - 本次融合训练完整数据（动作×组次、时长、热量、心率摘要）；
   - 近 4 周同部位/同动作历史摘要（每动作最近 3 次的重量×次数趋势与 PR）；
   - 近 7 天睡眠时长/深睡比例/HRV 状态/训练准备度；
   - 要求模型输出结构化 Markdown：完成质量 / 与历史对比 / 恢复评估 / 注意事项 四节；
3. 报告落 ai_report（含 model、token、成本），前端新增"AI 报告"入口可查看当日点评；
4. 生成失败（LLMError）写 job_run 告警，不阻塞同步主流程。

TDD 要求：
- prompt 组装函数纯函数化，测试其包含全部必需数据节；
- mock LLM 返回固定 Markdown，测试落库字段与前端查询接口；
- 测试 LLM 失败时 job_run 告警且同步状态仍为成功；
- 前端 AI 报告页组件测试。

完成后按 P0 第 9 条提交并输出完成报告。
```

### Sprint 3 评审与复盘

```
Sprint 3 评审与复盘。

1. 全部测试跑绿 + 覆盖率报告；
2. 逐条核对 US-5/US-6/US-9(AC2 记账部分) 验收标准；
3. 我提供真实凭据跑历史导入，完成后我告诉你导入天数与融合记录数，你核对数据合理性
   （如训记导入条数与我提供的预期是否同量级）；
4. 抽查 3 篇 AI 点评内容质量（数据引用是否真实、建议是否具体），列出改进点；
5. 更新 docs/TECH_DEBT.md，打 git tag "sprint-3"。
```

---

## Sprint 4：AI 建议、训记写回与身体数据（任务 V1-4～V1-7）

**Sprint 目标**：下次训练建议上线，写回确认流完整闭环，身体数据模块可用，前端 V1 完成。

### V1-4 下次训练建议

```
任务 V1-4：实现 services/ai.py 的下次训练建议（type='next_advice'）。

1. 触发：单次点评生成后连锁触发；
2. 输入：训记官方计划缓存（未来 30 天中下一次训练日的计划明细）+ 本次完成情况 + 恢复指标；
3. 输出要求：对照计划逐动作给出调整建议，粒度到「动作/重量/组数/次数」，
   并用 JSON 块输出机器可读的结构化建议（动作名、原参数、建议参数、理由），
   Markdown 正文给人看，JSON 给程序用；
4. 前端详情页展示两类建议：
   - 「可自动写回」：对已发生训练的 RPE/难度修正 → 每条带"生成写回预览"按钮；
   - 「需手动调整」：计划本身的修改 → 附训记 App 操作路径指引文案；
5. 动作名只允许使用 GitHub Foveluy/Xunji-movements 表中的标准中文名，
   组装 prompt 时把动作名表注入上下文约束模型。

TDD 要求：测试结构化 JSON 建议的解析与校验（非法动作名被拒）、两类建议的分类逻辑、
前端两类区块渲染与按钮行为。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V1-5 训记写回确认流（高危任务）

```
任务 V1-5：实现训记写回完整确认流。这是高危任务，PRD §5.4 的规则逐条落实。

1. 解除 xunji.upsert_trains 的强制 dry_run，但默认参数仍为 dry_run=True，
   只有确认接口内部调用才传 dry_run=False；
2. 后端接口：
   - POST /api/writeback/preview：输入建议 JSON，服务端先用 include_full_data=true 读原训练，
     生成"原值 → 新值"的 diff（保留 localid/start/end/note 全量元数据）返回；
   - POST /api/writeback/confirm：用户确认后执行真实写回，
     遵守 45s 写回限频（多个写回排队串行执行），
     成功后用服务端返回的标准化数据覆盖本地 xunji_train 缓存并重跑该日融合；
3. 前端：预览页展示 diff 表格（字段/原值/新值三列，变更高亮），
   「确认写回」按钮二次弹窗确认；
4. 每次写回写 job_run 留痕（请求体摘要+结果）。

TDD 要求：
- 测试 diff 生成保留全部元数据（构造含 note.trainColor/heartRate 的原训练）；
- 测试未确认时任何路径都不会真实外呼（spy 验证）；
- 测试限频排队：连续 3 个写回确认间隔 ≥45s；
- 测试写回后缓存覆盖与融合重跑被触发；
- 测试单日 >4 条/单训练 >15 动作/单动作 >20 组的请求被拒。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V1-6 前端 V1 完善

```
任务 V1-6：完善前端至 V1 完整形态。

1. 趋势页：近 4/12 周切换——每周总容量（吨）、各部位训练频次（堆叠柱状图）、
   体重/体脂曲线（佳明体成分数据，如有）、睡眠时长与训练容量散点；
2. AI 报告中心：按类型（点评/建议）与时间浏览全部 ai_report；
3. 历史导入进度页：调 /api/backfill/status 轮询展示进度条；
4. 设置页：LLM Key 配置（V1-1 的接口）、显示本月 LLM 调用次数与估算成本。

TDD 要求：新页面组件测试 + 新 API 的 pytest 用例；ECharts 数据装配函数纯函数化并单测；
npm run build 通过。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V1-7 身体数据模块（2026-08-03 需求新增）

```
任务 V1-7：实现身体数据记录模块（PRD US-12）。

1. 数据模型：按 PRD §4 新增 body_metric 表（Alembic 新迁移），UNIQUE(date, type)；
2. 后端 API：
   - POST /api/body-metrics（录入，按 date+type upsert，同日同类型覆盖）；
   - GET /api/body-metrics?type=&from=&to=（趋势查询）；
   - POST /api/body-metrics/{id}/sync-xunji（仅 weight/bodyfat 允许）：
     调训记身体数据 API，严格三段式——dry_run:true 取 res.summary → 返回前端展示 →
     前端确认后带 confirmed:true + dry_run:false 执行；成功后置 synced_to_xunji=TRUE；
   - 身高/血压/血糖调同步接口一律 400 拒绝（训记无对应类型）；
3. 前端"身体数据"页：四类指标录入表单（血压为收缩/舒张两个字段）、
   各指标 ECharts 趋势曲线、体重曲线与训练容量同屏对照图、
   "同步到训记"按钮带摘要弹窗确认、"仅本地"标注；
4. 首次使用引导：无身高记录时提示先录身高；
5. 修改 services/ai.py：单次点评与周/月复盘的 prompt 组装纳入近 4 周体重趋势；
6. 环境变量 XUNJI_BODY_API_KEY 加入 config 与 .env.example；
   注意训记腰围字段拼写是 weist（如未来扩展围度不要"修正"它）。

TDD 要求：
- upsert 幂等与同日覆盖测试、四类指标边界值校验（如血糖 0.5–40 mmol/L 合理区间）；
- 同步三段式流程测试：mock 训记接口，验证未确认前绝不发送 confirmed:true、
  确认后写库标记、身高/血压/血糖同步被拒；
- AI prompt 组装测试：包含体重趋势节；
- 前端录入表单与趋势图组件测试。

完成后按 P0 第 9 条提交并输出完成报告。
```

### Sprint 4 评审与复盘

```
Sprint 4 评审与复盘（V1 里程碑）。

1. 全部测试跑绿 + 覆盖率报告；
2. 逐条核对 US-7/US-8/US-12 全部验收标准，重点演示一遍写回确认流（用我的一次真实训练做 dry_run 预览），
   以及体重录入→趋势图→同步训记三段式确认流；
3. 高危项复查：grep 全仓确认无硬编码密钥、写回默认 dry_run、限频装饰器覆盖全部训记调用；
4. 更新 docs/TECH_DEBT.md，打 git tag "v1.0"。

附：以下数据已经过实地验收核实（2026-08-07），复核时作为对照锚点，若复跑结果不一致须查明原因再归档：
- 测试基线：后端 426 passed / 3 skipped（3 条均为显式门禁的集成测试），总覆盖率约 94.4%；
  前端 Vitest 97 passed；npm run build 通过；oxlint 0 错误。
- V1-4 下次建议：训记计划新周期 2026-08-07 ~ 08-13 为 active，真实生成 next_advice（ai_report 报告 #4，
  DeepSeek，8123+2383 tokens），内容与训记计划 API 逐条对照一致；V1-4-FIX 修复了 fetch_plan 的
  date 序列化 bug（mock 未暴露、真实链路翻车），回归夹具为真实 API 抓取。
- V1-5 写回：已做过一次真实写回并服务器核验——2026-08-03「背·二头·2」宽距高位下拉第 1 组标 RPE 8，
  服务器回查 6 动作 22 组分毫不差（三级深合并，V1-5-FIX 修复了整体替换丢数据 bug）；
  真实 preview 145 行 diff 中 changed=true 恰好 1 行。
- V1-6 趋势：周容量 API 返回与手算一致（22.32 吨）。
- V1-7 身体数据：本地 body_metric 2 条（2026-08-07 weight 88.0kg 已同步 / height 178cm 仅本地）；
  真实同步三段式已实测——预览不发写请求、confirmed 写入、训记服务器回查 2026-08-07 weight=88 落账
  （记录 id 13063090）；身高同步被正确 400 拒绝（训记身体 API 仅支持 weight/bodyfat/围度）。
- 数据存量：ai_report 4 条；job_run 有 writeback/health_check 成功留痕。
- 已知遗留（属设计内，不阻塞 v1.0）：真实训记身体数据 API 的录入侧联调仅做了体重一种类型；
  echarts chunk >500kB 警告未处理；Kimi Provider 仍为桩（V2-1 补）。
```

---

## Sprint 5：多模型与周期复盘（任务 V2-1～V2-3）

**Sprint 目标**：Kimi 接入后三模型可切换，周/月复盘自动生成，截图识别兜底可用。

### V2-1 多模型扩展（2026-08-06 修订：DeepSeek/MiniMax 已在 V1-1 接入，本任务补 Kimi）

```
任务 V2-1：扩展 llm.py 接入 Kimi（DeepSeek/MiniMax 已在 V1-1 实现），实现设置页三模型切换。

1. 实现 Kimi Provider（OpenAI 兼容端点 https://api.moonshot.cn/v1），替换 V1-1 的 NotImplementedError 桩；
   KIMI_API_KEY 届时由我申请后填入 .env；
   【2026-08-07 模型核查】默认模型用 kimi-k2.6（视觉+文本、256k、思考/非思考模式），备选 kimi-k3；
   k2 系列（含 kimi-k2-0905-preview）已于 2026-05-25 下线，kimi-k2.5 与 moonshot-v1 全系列
   （含 *-vision-preview）2026-08-31 下线，一律不要使用；接入时用 GET /v1/models 实测返回的
   模型列表核对一次，若 k2.6 也不在列则以平台最新通用模型为准并在完成报告中说明；
   【2026-08-07 Key 实测】真实 Key 已验证可用，实测发现 kimi-k2.6 默认开思考模式：
   reasoning_tokens 计入 completion_tokens（实测 max_tokens=100 时 99 个全被推理吃光、
   正文为空）——调用时 max_tokens 必须留足推理余量（≥2048）；响应可能带 reasoning_content
   字段，落库只取 content，推理内容不入库（与 MiniMax <think> 剥离逻辑并列处理）；
2. 设置页可切换默认模型（DeepSeek/MiniMax/Kimi）；某模型连续失败 2 次时界面提示可一键切备用模型重试；
3. 月度成本汇总 API：GET /api/llm/monthly-usage 按 provider 分组返回 token 与估算成本
   （顺手把 LLM_PRICES_JSON 里 DeepSeek/MiniMax 的刊例占位价按各平台当前定价页校准，Kimi 价格
   从 https://platform.kimi.com/docs/pricing 取）；
4. 截图识别走 Kimi 多模态模型（kimi-k2.6/k3 原生支持视觉，base64 图片输入，OpenAI 兼容的
   image_url 消息格式），封装 vision_extract(image_bytes, prompt) 方法；
   不要再用 moonshot-v1-*-vision-preview（8/31 下线）。

TDD 要求：mock Kimi 接口；测试切换默认模型后路由正确、失败降级重试、
月度汇总分组正确、vision_extract 请求体格式（base64 编码正确）。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V2-2 周期复盘

```
任务 V2-2：实现周复盘与月复盘（services/ai.py 增加 weekly/monthly）。

1. 调度：每周日 21:13 周复盘（type='weekly'），每月 1 日 09:23 月复盘（type='monthly'）；
2. 周复盘输入：本周全部融合训练（频率/部位分布/总容量/PR 事件）+ 睡眠与 HRV 趋势 +
   上周复盘中的建议（供模型自评建议执行情况）；
   月复盘输入：月度汇总 + 训记计划完成率 + 体成分变化；
3. 输出 Markdown 报告（含 ECharts 可渲染的数据块），支持导出 Markdown 与 PDF
   （PDF 用 reportlab 或 weasyprint，CJK 字体需正常）；
4. 前端复盘中心：周/月报告列表 + 详情 + 导出按钮；生成完成时前端轮询提示。

TDD 要求：prompt 组装测试（含"上周建议回读"）、调度时间配置测试、
PDF 导出测试（生成文件可打开且包含中文字符）、导出接口测试。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V2-3 截图识别兜底

```
任务 V2-3：实现截图识别补录通道。

1. 前端上传页：拖拽上传训记/佳明截图（可多图）；
2. 后端：调 vision_extract，prompt 要求输出严格 JSON（datestr/title/movements[sets]/duration/calories），
   服务端做 JSON Schema 校验，不合法自动重试 1 次（附错误反馈让模型修正）；
3. 识别结果入预览页（不落库），展示识别字段与置信提示，用户编辑修正后点「确认入库」：
   - 训记类数据写 workout（match_status 按现有规则重跑匹配）；
4. 用我提供的训记标准报表示例图（根目录 素材/训记分享报表示例.png）做基准测试，
   字段识别准确率 ≥ 95%（动作名/重量/次数逐字段比对）。

TDD 要求：Schema 校验与自动修正重试测试、确认入库触发重匹配测试、
基准图识别的 integration 手动测试（真实视觉 API，默认跳过）。

完成后按 P0 第 9 条提交并输出完成报告。
```

### Sprint 5 评审与复盘

```
Sprint 5 评审与复盘。

1. 全部测试跑绿 + 覆盖率报告；
2. 逐条核对 US-9/US-10/US-11 全部验收标准；
3. 用基准截图现场演示识别准确率；
4. 更新 docs/TECH_DEBT.md，打 git tag "sprint-5"。

附：以下数据已经过实地验收核实（2026-08-10），复核时作为对照锚点，若复跑结果不一致须查明原因再归档：
- 测试基线：后端 538 passed / 4 skipped（4 条均为显式门禁集成测试），总覆盖率 94.69%；
  前端 Vitest 113 passed（16 文件）。
- V2-1 Kimi 接入：kimi-k2.6 真实调用成功，记账精确（17+286 tokens，¥0.0078325 与单价表 6.5/27 手算一致）；
  定价已与官方页核对（kimi-k2.6 ¥6.5/¥27 每 1M tokens）；k2 系列已下线，禁用模型清单见 PRD §6.3。
  【环境坑已修复】OS 级残留旧 KIMI_API_KEY 曾遮蔽 .env 导致 401，config.py 已改 override=True（commit abf5229）。
- V2-2 周/月复盘：真实生成周复盘（ai_report #5，2026-08-03~09，DeepSeek 571+1287 tokens，¥0.003145）；
  数据经逐项核验 100% 准确（3 次训练、总容量 10,710kg 与手算一致、五部位容量加总自洽、
  杠铃深蹲 PR 40kg vs 历史 35kg 属实——注意哈克机反向深蹲 70kg 是另一动作，精确动作名匹配正确）；
  幂等重触发 skipped 且 llm_call 不增；调度为周日 21:13 / 每月 1 日 09:23（复盘上月）；
  PDF 导出实测 2 页中文正常（STSong-Light）。已存样例 周复盘_2026-08-03_08-09.pdf。
  【环境坑已修复】requirements 新增 reportlab/pypdf 后 venv 未同步导致收集报错，已补装。
- V2-3 截图识别：基准图（素材/训记分享报表示例.png）真实 Kimi 视觉识别复现 53/54 = 98.1%，
  唯一未命中为集中弯举第 2 组重量；生产路径（不传 session）实测记账 purpose=vision_extract、
  4525+1414 tokens、¥0.067591（单张识别成本约 ¥0.068）；基准测试计费落 test 库属设计内。
- 生产服务已于 2026-08-10 11:25 重启（新代码已生效，新路由 401 非 404 验证）；
  调度器 weekly_review/monthly_review 注册情况以 12:11 health_check 与 22:47 daily_sync 的 job_run 为证。
- 已知遗留（不阻塞 sprint-5）：识别预览态刷新丢失；确认入库未做幂等（按钮置灰兜底）；
  PDF 中 echarts 块以代码文本呈现不渲染图表；DeepSeek 近期将涨价并引入峰谷定价，价格表届时再校准。
```

---

## Sprint 6：加固与部署（任务 V2-4～V2-6）

**Sprint 目标**：降级通道与告警就绪，全系统上云服务器，桌面套壳（可选）。

### V2-4 降级通道与监控告警

```
任务 V2-4：实现佳明文件导入降级通道与告警推送。

1. 实现 garmin_adapter.import_fit_file(path)：解析 FIT/TCX（用 fitparse/gpxpy 类库），
   落库 garmin_activity 并触发该日重匹配；前端提供上传入口；
2. 健康检查发现佳明/训记连续 3 次失败 → 推送告警（Server酱或 SMTP 邮件，配置化），
   告警文案含失败接口、错误摘要、建议操作（如"请手动导出 FIT 文件上传"）；
3. 每日数据库自动备份（SQLite 文件复制+日期戳，保留 30 天滚动清理）。

TDD 要求：FIT 解析用样本文件测试落库与重匹配触发、告警阈值与去重
（同故障不重复轰炸，30 分钟冷却）、备份滚动清理测试。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V2-5 云端部署

```
任务 V2-5：编写生产部署方案并生成全部部署文件。

1. docker-compose.yml：backend（FastAPI+APScheduler）、frontend（构建产物 nginx 托管）、
   postgres（数据卷）、backup（每日备份 cron 容器）；数据库从 SQLite 迁移到 PostgreSQL
   （DATABASE_URL 切换，Alembic 迁移重放验证）；
2. 全站 HTTPS（Caddy 或 nginx+certbot 反代），登录口令强制；
3. .env 模板区分开发/生产；密钥全部经环境变量注入；
4. 编写 docs/DEPLOY.md：从零到可访问的完整步骤（含佳明 token 首次登录的交互处理）；
5. 输出部署前检查清单脚本 scripts/preflight.sh（端口/环境变量/迁移状态/磁盘）。

TDD 要求：Alembic 在 PostgreSQL 上的迁移测试（用 testcontainers 或 CI 服务容器）、
生产配置加载测试。

完成后按 P0 第 9 条提交并输出完成报告。（实际部署操作由我执行，你输出指令我逐步照做。）
```

### V2-6 桌面套壳（可选）

```
任务 V2-6：用 Tauri 将前端打包为 Windows 桌面客户端。

1. Tauri 壳内加载云端前端地址（不做本地后端）；
2. 窗口最小尺寸适配看板布局，记住窗口位置；
3. 打包出 .msi 安装包并写安装说明。

测试要求：构建通过、安装包生成；功能验证以云端前端自身测试覆盖为准。

完成后按 P0 第 9 条提交并输出完成报告，打 git tag "v2.0"。
```

### Sprint 6 评审与项目总结

```
Sprint 6 评审与项目总结。

1. 全部测试跑绿 + 最终覆盖率报告；
2. PRD 全部用户故事验收标准总核对表（达成/未达成/偏差说明）；
3. 输出项目总结：架构回顾、技术债最终清单、运维手册要点（备份恢复/告警响应/密钥轮换）；
4. 打 git tag "v2.0-final"。
```

---

## Sprint 7：生产使用反馈修复与计划功能（任务 V2-7～V2-8）

**Sprint 目标**：修复 2026-08-10 云端上线首日实地使用发现的缺陷，补齐手动同步入口与训练计划页。

**背景锚点（2026-08-10/11 实地验收发现，修复后须回归验证）**：
- 生产环境：腾讯云 118.24.143.172，docker compose 栈（postgres/backend/frontend:8080/backup），
  访问入口 http://118.24.143.172:8080；部署更新方式：`cd ~/fitness-app && git pull && docker compose up -d --build backend frontend`；
- 缺陷 1：POST /api/sync/{date} 未鉴权（裸奔接口）；
- 缺陷 2：同步为同步阻塞执行，经 nginx 反代 60s 超时返回 504（实际后端跑完了，job_run 有 success 留痕）；
- 缺陷 3：训记同日缓存策略（fetched_at >= 当日零点即命中）导致当天补录的组数拉不下来，
  08-10 训练实际 8 动作 27 组，只同步到 4 组半；force_refresh 重拉后原始数据补齐，
  但 fuse_workout 只新建不更新、matcher 对已配对记录幂等跳过，融合数据不刷新、AI 报告不重生成；
- 缺陷 4：WorkoutDetailPage 只展示 next_advice，不展示 session_review（报告中心可见，详情页不可见）。

### V2-7 手动同步与首日缺陷修复

```
任务 V2-7：手动同步功能 + 上线首日四处缺陷修复。

## 缺陷修复（先修，严格 TDD 先红后绿）
1. POST /api/sync/{date} 挂 require_auth（现为裸奔接口）；
2. 同步改后台线程异步化：POST /api/sync/{date} 立即返回 202 + job 标识，
   新增 GET /api/sync/status 查询进行中/最近结果；同日期已有同步在跑返回 409 防重；
3. 当日缓存修复：daily_sync / 手动同步的日期为"今天"时，训记读取必须
   force_refresh=True 绕过同日缓存（历史日期保持缓存命中不变）；
4. 重融合缺陷：fuse/match 流程中，已配对 workout 对应的 xunji_train 若
   fetched_at 晚于 workout.updated_at，须就地更新 workout.movements_json
   （复用 fuse._extract_movements，不新建 workout 行），并删除该 workout 当日
   已有的 session_review / next_advice 后重生成，保证 AI 报告基于最新数据；
5. WorkoutDetailPage 新增 SessionReviewSection：进入页面时
   GET /api/ai-reports?date={date}&type=session_review，有则展示 Markdown 正文
   （与既有 next_advice 区块并列）。

## 新功能
6. 前端日历页（/ 或 CalendarPage）加「立即同步今天」按钮：
   点击 → POST /api/sync/{today} → 轮询 GET /api/sync/status →
   完成后 toast 展示摘要（xunji_trains/garmin_activities/workouts/ai_reviews 数量）；
   同步中按钮 loading 置灰；
7. 部署配套：frontend/nginx.conf 的 /api/ location 加 proxy_read_timeout 300s
   （同步类长请求兜底，异步化后仍有导出等慢接口）。

## 测试要求
- 后端：sync API 鉴权 401、异步化 202/409/status 流转、当日 force_refresh、
  重融合就地更新（不新建行、movements_json 刷新、旧 AI 报告删除重生成）、
  全部 mock 零真实外呼；
- 前端：按钮流转（loading→toast）、SessionReviewSection 有/无报告两种渲染；
- 完成后全量复跑 backend pytest（附覆盖率）、frontend vitest、oxlint、
  npm run build，均无回归。

完成后按 P0 第 9 条提交并输出完成报告。
部署到云端（我执行）：cd ~/fitness-app && git pull && docker compose up -d --build backend frontend
```

### V2-8 训练计划页 + 计划级 AI 点评

```
任务 V2-8：训练计划页 + 计划级 AI 点评。

## 背景
- 后端已有 sync_plan_cache（services/sync.py，每日 23:03 调度），将训记官方计划
  缓存在 xunji_plan 表（plan_ref / plan_json / date_from / date_to）；
- 已有 services/ai.py 的 query_next_plan_day / build_next_advice_prompt /
  generate_next_advice（type='next_advice'），目前只能从某个 workout 触发；
- 训记官方计划 API 仅支持读取（素材/训记key_新版.txt 明确），
  禁止任何"修改计划"的写回尝试；AI 修改建议仅作展示，提示用户在训记 App 手动调整。

## 任务 1：计划查询 API（backend/app/api/plans.py）
1. GET /api/plans/upcoming?days=30 —— 从 xunji_plan 缓存读出未来 N 天计划日程，
   逐日返回 {date, plan_ref, plan_name, title, movements:[{name, target_sets}]}，
   休息日（无计划日）也要能区分；复用 services/ai.py 的缓存解析逻辑，
   把 _parse_json / _normalize_plan_movement 提取为可复用函数（不许复制粘贴）；
2. POST /api/plans/refresh —— 手动触发 sync_plan_cache（后台线程，立即返回 202；
   已在跑返回 409），挂 require_auth；
3. 全部端点鉴权；单测用 fake xunji client，零真实外呼。

## 任务 2：计划级 AI 点评（plan_review）
1. services/ai.py 新增 generate_plan_review(session, target_date, chat_fn=None)：
   - 从缓存取 target_date 的计划日（无计划返回 None 并给可读原因）；
   - 上下文：该计划日动作清单 + 最近一次同类型 workout 完成情况 +
     近 4 周同部位容量趋势 + 恢复指标（复用现有 query_* 函数）+ 近 4 周体重趋势；
   - 输出 Markdown 点评 + ```json 围栏块（schema: plan_review_v1）：
     {"schema":"plan_review_v1","plan_date":"YYYY-MM-DD","modifications":[
       {"movement":"<标准动作名>","field":"weight|reps|sets|add|remove",
        "from":"...","to":"...","reason":"..."}]}；
   - 动作名过标准动作名白名单校验（复用 load_movement_names），失败重试 1 次，
     仍失败抛错不落库；
   - 落库 ai_report type='plan_review'，period_start=period_end=target_date；
   - 幂等策略为"覆盖"：同日已有 plan_review 先删旧再生成（用户会反复调整，
     与 next_advice 的跳过策略不同）；
2. API：POST /api/plans/review/{date}（后台线程 + 409 防重 + 鉴权）、
   GET /api/plans/review/{date}（取最新一条，无则 404）；
3. 调度不改：plan_review 只手动触发，不进 scheduler。

## 任务 3：前端"训练计划"页（/plans）
1. 路由 + 导航新增「训练计划」页：
   - 未来 30 天计划列表（按日分组卡片：日期/计划名/标题/动作清单含目标组数，
     休息日弱化展示）；
   - 「刷新计划」按钮 → POST /api/plans/refresh，轮询后刷新列表；
   - 每个计划日卡片「AI 点评」按钮 → POST /api/plans/review/{date}，
     生成中 loading，完成后卡片下方展示点评；
   - 点评展示：Markdown 正文 + 「修改建议」结构化表格
     （动作 | 调整项 | 原计划 | 建议改为 | 理由），醒目提示
     "计划接口只读，请在训记 App 中手动调整"；
   - 已有 plan_review 的计划日进入页面即加载展示；
2. 训练详情页 next_advice 展示保持不变，不重复建设。

## 测试要求
- 后端：计划解析（list/get 两种缓存结构、status=ended、休息日）、
  refresh 409/鉴权、plan_review 生成/幂等覆盖/白名单校验失败重试/无计划日、
  API 401；全部 mock；
- 前端：列表渲染、按钮流转、修改建议表格渲染、无计划日提示；
- 完成后全量复跑 backend pytest（附覆盖率）、frontend vitest、oxlint、
  npm run build，均无回归。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V2-9 移动端报告阅读体验（底部抽屉）

```
任务 V2-9：修复移动端"点报告后内容出现在页面底部、需长滚动查看"的问题，
AI 报告页与复盘中心改为底部抽屉（Bottom Sheet）展示详情。

## 背景（2026-08-11 真机反馈，代码已核实）
- AIReportsPage 与 ReviewsPage 均为桌面 master-detail 布局
  （grid md:grid-cols-3，左列表 md:col-span-1，右详情 md:col-span-2），
  手机塌缩为单列后详情排在整个列表下方，必须长滚动才能看到；
- 全系统排查：仅这两页有此问题（计划页点评为卡片内就地展开，正常）；
- 另有 4 处表格无横向滚动容器，窄屏挤压：PlansPage 修改建议表、
  SettingsPage 月度用量表、WorkoutDetailPage 组次表、
  NextAdviceSection 建议 diff 表。

## 任务 1：共享底部抽屉组件
新增 components/BottomSheet.jsx：
- fixed 底部弹层：rounded-t-2xl、max-h-[85vh]、内容区 overflow-y-auto；
- 半透明背板，点背板关闭；右上角关闭按钮；打开时锁 body 滚动；
- 顶部标题栏插槽 + 底部操作区插槽（供放上一篇/下一篇按钮）；
- 不做滑动手势（保持简单）。

## 任务 2：AIReportsPage 与 ReviewsPage 接入
- 新增 hooks/useIsMobile.js（matchMedia '(max-width: 767px)'，监听变化）；
- 移动端（<md）：点击报告卡片 → BottomSheet 展示完整详情
  （复用现有详情渲染：头部元信息 + SimpleMarkdown/ReviewContent），
  抽屉底部放「上一篇 / 下一篇」按钮，在当前筛选结果列表内循环外的
  首尾处禁用；原右侧详情栏在移动端 hidden md:block，桌面端行为零变化；
- 桌面端（≥md）：保持现有左右分栏，不渲染抽屉；
- ReviewsPage 的导出 Markdown/PDF 按钮在抽屉内同样可用。

## 任务 3：表格横向滚动修复
上述 4 处表格外层包 <div className="overflow-x-auto">，表格本体加
min-w-max 或 whitespace-nowrap 按现有样式微调，保证窄屏可左右滑动不挤压。

## 测试要求
- useIsMobile：matchMedia mock 两种断点；BottomSheet：打开/背板关闭/
  按钮关闭/body 滚动锁；两页移动端点击卡片出抽屉、上一篇/下一篇切换与
  首尾禁用；桌面端现有测试零回归（不得破坏既有断言）；
- 表格容器 class 断言；
- 完成后全量复跑 backend pytest + frontend vitest + oxlint + npm run build。

完成后按 P0 第 9 条提交并输出完成报告。
真机验收（部署后我做）：手机浏览器开 AI 报告页与复盘中心，点击报告
弹抽屉、切换上一篇/下一篇、表格左右滑动，均正常即通过。
```

### Sprint 7 评审与复盘

```
Sprint 7 评审与复盘。

1. 全部测试跑绿 + 覆盖率报告；
2. 回归验证 Sprint 7 背景锚点的四处缺陷：sync API 未带 token 返回 401；
   云端手动同步当天数据（force_refresh 生效，组数与训记 App 一致）；
   重融合后 AI 报告基于最新数据；训练详情页可见 session_review；
3. 用云端真实计划缓存（xunji_plan）联调训练计划页与 plan_review 生成；
4. 更新 docs/TECH_DEBT.md，打 git tag "sprint-7"。
```

---

## Sprint 8：安卓 APP（任务 V3-1～V3-2）

**Sprint 目标**：用 Capacitor 把现有前端打包为安卓 APP（.apk），并做原生手感打磨。

**背景锚点（2026-08-11 方案评审结论）**：
- 选型：Capacitor WebView 套壳。PWA 被否（无域名、HTTP 环境 Service Worker 不可用，
  上域名需 ICP 备案数周）；原生重写被否（单用户场景性价比为零）；
- 加载模式：`server.url` 直连 http://118.24.143.172:8080（WebView 与后端同源，
  零 CORS 改造；前端更新走 docker 重建即生效，不必重发 APK）；
- 前提：HTTP 明文流量需在 AndroidManifest 开 usesCleartextTraffic；
- 打包环境：用户 Windows 本机（Android Studio + JDK 17），云服务器不参与打包；
- 前端已有 useIsMobile hook（matchMedia 767px）、BottomSheet、汉堡菜单（V2-9b）；
- TDD 边界：Android 构建不进 CI（无 SDK）；前端逻辑测试维持 vitest，
  Capacitor 配置用 Node 脚本断言校验。

### V3-1 Capacitor 打包出 APK

```
任务 V3-1：用 Capacitor 将前端打包为安卓 APP，产出可安装的 release APK。

1. 在 frontend/ 接入 Capacitor：@capacitor/core @capacitor/cli @capacitor/android；
   capacitor.config.ts 关键配置：
   - appId: 'com.fmmf.fitnesshub'，appName: '健身看板'；
   - server.url: 'http://118.24.143.172:8080'（读手册 Sprint 8 背景锚点，
     直连生产服务，webDir 仍配 dist 以便将来切换离线壳模式）；
   - server.androidScheme 保持默认 http（配合 cleartext）；
2. Android 工程适配：
   - AndroidManifest.xml 开 android:usesCleartextTraffic="true"（HTTP 明文）；
   - 应用图标与启动屏：制作 1024×1024 品牌图标（indigo #4F46E5 底 + 白色
     哑铃/闪电简约图形）与 splash，用 @capacitor/assets 生成全尺寸；
     图标源文件存 frontend/assets-src/ 入库；
   - android/ 目录入库，但 local.properties、*.keystore、build/ 入 .gitignore；
3. 构建：gradlew assembleDebug 先出 debug APK 验证；再 assembleRelease
   （debug 签名即可，单用户不上架，不做签名加固）；
4. 文档：docs/ANDROID.md 写清本机环境要求（Android Studio、JDK 17、
   SDK Platform 34）、打包命令、APK 输出路径、安装方法（微信传文件/ adb install）；
5. Node 校验脚本 frontend/scripts/check-capacitor-config.mjs：
   断言 capacitor.config.ts 的 appId/appName/server.url、
   AndroidManifest 的 usesCleartextTraffic、图标文件存在；
   挂进 npm scripts（test:capacitor），本地与 CI 都跑。

TDD/测试要求：前端既有 158 用例零回归 + oxlint + build 通过；
校验脚本自身用例（篡改配置应报红）；Android 构建不进 CI。

完成后按 P0 第 9 条提交并输出完成报告，附 APK 输出路径与首屏截图。
```

### V3-2 原生手感打磨（底部 Tab + 返回键 + 状态栏）

```
任务 V3-2：安卓原生手感打磨——底部 Tab 导航、返回键语义、状态栏与安全区。

1. 移动端底部 Tab 栏（本次核心视觉改动）：
   - 新增 components/BottomTabs.jsx，仅 <md 渲染（复用 useIsMobile），
     五个 Tab：训练日历(/) 训练计划(/plans) AI报告(/ai-reports)
     趋势(/trends) 我的(/settings)；
   - 每 Tab 为 图标+文字，激活态 indigo-600，未激活 gray-500；
     图标用内联 SVG（lucide 风格线条图标，不引新依赖）；
   - fixed 底部 + pb-[env(safe-area-inset-bottom)] 安全区；
     主内容区在移动端加对应 bottom padding 防遮挡；
   - 原汉堡菜单保留给次级入口（待确认队列/复盘中心/身体数据/截图导入/
     文件导入/历史补录），汉堡按钮移至右上角；
   - 桌面端布局零变化；
2. 返回键语义（@capacitor/app）：
   - App.addListener('backButton')：抽屉/弹层打开时先关弹层；
     非根路由 history.back()；根路由则 App.minimizeApp()（退到后台不杀进程）；
   - 逻辑抽为 hooks/useAndroidBackButton.js，Web 环境下安全空转
     （Capacitor.isNativePlatform() 判断，浏览器零影响）；
3. 状态栏（@capacitor/status-bar）：浅色背景 + 深色文字与现有
   UI 一致；沉浸式边到边布局，内容区 pt-[env(safe-area-inset-top)]；
4. 风格收敛：保持现代简约——圆角统一 rounded-lg/xl、阴影仅 shadow-sm、
   主色 indigo-600 不动；本任务不做大范围换肤。

TDD/测试要求：BottomTabs 渲染/激活态/安全区 class 断言、
useAndroidBackButton 三分支（弹层优先/非根返回/根路由最小化）mock
@capacitor/app 测试、汉堡菜单次级入口可达性测试；
既有测试零回归 + oxlint + build + test:capacitor 全绿。

完成后按 P0 第 9 条提交并输出完成报告。
真机验收（我做）：APK 安装后五 Tab 切换、返回键三分支、抽屉与 Tab 栏
不互相遮挡、状态栏融合、横滑表格不与返回手势冲突。
```

### Sprint 8 评审与复盘（范围已扩至 V3-1～V3-6，2026-08-15 真机验收全过）

```
Sprint 8 评审与复盘（范围：V3-1 Capacitor 打包 → V3-6 海报丰富化，
真机验收已于 2026-08-15 全部通过：五 Tab / 返回键 / 抽屉 / 状态栏 /
趋势图移动端 / 报告渲染 / 评分徽章 / 海报预览 / 微信朋友圈分享链路）。

1. 复跑全部测试确认无回归，基线：
   - 后端 671 passed / 6 skipped / 总覆盖率 93.48%（门禁 85%）；
   - 前端 vitest 240 passed + oxlint 0 error（1 个存量 warning T28 不动）
     + npm run build + npm run test:capacitor；
2. git log 核对 V3-1～V3-6 六个提交齐全（7e2513f / 0858b2b / 62791fe /
   9f9e1ae / 305a19b，V3-2 提交号以实际 log 为准），HEAD 与工作区干净；
3. 更新 docs/TECH_DEBT.md：
   - 销项：报告 AI 符号裸奔、海报内容单薄、有氧指标 "-" 占位、
     无评分海报大片空白；
   - 新增 T29：全量 vitest 出现 1 例偶发 flake（V3-6 期间重跑两轮全绿
     未定位），后续观察，复现即修；
   - 新增 T30：无 PR/无亮点的有氧海报，点评卡与水印间空白略大
     （观感问题，不阻断）；
   - 保留：T23/T24/T25/T28；
4. 记录 APK 信息进 TECH_DEBT 附录：release APK 约 3.4MB（debug 签名），
   冷启动耗时真机实测记录；
5. 打 annotated git tag "sprint-8"；
6. 推送：git push origin main && git push origin sprint-8
   （注意：GitHub 目前落后 3 个提交 62791fe/9f9e1ae/305a19b，
   本次推送应一并补齐；若网络不通记录为阻塞项，用 bundle 兜底）。

输出 Sprint 8 评审报告：测试基线核对表 / 六个任务完成度逐项确认 /
TECH_DEBT 变更摘要 / tag 与推送结果。
```

### V3-3 趋势图表移动端适配（2026-08-13 真机截图确诊，已验收）

```
任务 V3-3：趋势图表移动端适配。utils/trends.js 与 bodyMetrics.js 各构建函数
加 { mobile } 可选参（共享 applyMobile helper：axisLabel rotate45/MM-DD 裁剪、
legend type:scroll 小字号、grid {40,12,56,48}、去 yAxis.name），桌面缺省零变化；
TrendsPage/BodyMetricsPage 用 useIsMobile 传参；主内容区 pb-24→pb-32 防
BottomTabs 遮挡。已完成并验收（192 用例全绿，commit 0858b2b）。
```

### V3-4 报告渲染去 AI 味 + AI 评分体系

```
任务 V3-4：修复 Markdown 语法符号裸奔 + 给单次点评加评分。

## 背景（2026-08-13 真机截图确诊）
- SimpleMarkdown 只解析 # 与 ##，报告里的 ###/#### 标题、**加粗**、
  --- 分隔线全部以原始符号显示，观感"太 AI"；
- 用户要求：AI 标识全部去掉（渲染层彻底解决，存量报告立即生效）；
- 新增评分：每次训练点评给出总分，后续用于分享海报展示。

## 任务 1：SimpleMarkdown 全语法渲染（渲染层，存量报告立即生效）
- ###/#### → h3/h4 样式；**bold** 行内解析（含一行多段加粗）；
- --- → 分隔线；- / 数字列表 → 列表样式；``` 围栏块维持现有 pre；
- 段内行内代码 `code` 样式；链接若出现按纯文本处理即可；
- 保持零依赖（手写解析，不引 markdown 库）；
- ReviewContent / SessionReviewSection / NextAdviceSection 全部受益。

## 任务 2：session_review 评分（后端）
- build_session_review_prompt 增加输出要求：正文后附 ```json 围栏块，
  schema session_review_v1：
  {"schema":"session_review_v1","score":0-100 整数,
   "subscores":{"completion":0-100,"intensity":0-100,"recovery_fit":0-100},
   "one_liner":"一句 30 字以内口语化点评（供分享海报用，禁 markdown 符号）"}
- 解析校验（score 0-100 整数、三个子分 0-100、one_liner ≤40 字），
  失败重试 1 次仍败则降级：仅落 markdown 正文、score 置 null 不阻断；
- ai_report 表加 score/one_liner/subscores_json 字段（Alembic 迁移）；
- API：/api/ai-reports 响应带 score/one_liner；报告卡片与详情展示评分徽章
  （≥90 绿 / 75-89 indigo / 60-74 黄 / <60 红）；
- 存量报告无 score 时显示「重新生成」按钮：删除当日旧 session_review 后
  调 run_daily_reviews 重生成（复用 V2-7b 逻辑），鉴权 + 409 防重。

## 测试要求
- SimpleMarkdown 全语法用例（含混排、嵌套符号边界）；评分解析/校验/降级/
  重试；迁移往返；重新生成按钮流转；全量回归。

完成后按 P0 第 9 条提交并输出完成报告。
```

### V3-5 朋友圈分享海报

```
任务 V3-5：训练分享海报生成与原生分享（依赖 V3-4 的评分字段）。

## 需求（用户原话）
"生成可以发朋友圈的图片，简单点评，一张图展示最重要内容，
最终分数非常方便地展示在朋友圈。"

## 实现
1. utils/poster.js：Canvas 2D 手绘 1080×1440（3:4）海报，零依赖：
   - 顶部品牌条（健身看板 + 日期）；
   - 中部大号评分环（0-100，圆环进度 + 分数，配色按 V3-4 分档）；
   - 训练标题 + 关键指标行（总容量/时长/热量，有 PR 加 PR 角标）；
   - one_liner 口语点评（无 score 的旧报告降级为无评分布局）；
   - 底部「由 健身看板 生成」水印条；
   - 现代简约风：白底/圆角卡片/主色 indigo-600，字体用系统默认栈；
2. 报告详情（抽屉与桌面）+ 训练详情页点评区块加「分享海报」按钮：
   生成 → 预览弹层（图 + 操作按钮）；
3. 分享出口：
   - 原生端（Capacitor.isNativePlatform()）：@capacitor/share +
     @capacitor/filesystem 写缓存文件后调系统分享面板（可直发微信/朋友圈）；
   - 浏览器端降级：触发 PNG 下载；
   - 新插件需 npx cap sync 并重新 assembleRelease（完成报告附新 APK 路径）；
4. AndroidManifest 不需要额外权限（缓存目录分享走 FileProvider，
   Capacitor 模板已内置，如发现缺失再补）。

## 测试要求
- poster.js 各数据形态（有/无评分、有/无 PR、长 one_liner 截断）：
  jsdom 下 mock canvas 2D context，断言绘制调用序列关键参数
  （分数文本、颜色分档、标题）；
- 按钮流转（生成中→预览→分享/下载）；原生/浏览器分支 mock 测试；
- 全量回归 + test:capacitor；APK 重新打包（原生插件变更）。

完成后按 P0 第 9 条提交并输出完成报告，附海报效果图。
真机验收（我做）：生成海报 → 分享到微信 → 朋友圈展示效果确认。
```

### V3-6 海报内容丰富化

```
任务 V3-6：分享海报内容丰富化（2026-08-13 真机反馈"内容太简单"）。

## 确诊问题
1. 无评分旧报告的降级布局中部大片空白（评分环/子分/点评全缺）；
2. 有评分版内容也太单薄：PR 只有一个角标无明细，无动作亮点；
3. 有氧训练（跑步等）指标行「总容量」显示 "-" 观感差（08-15 真机截图）：
   有氧应展示 距离/时长/热量（有平均心率再加一栏），力量才展示容量。

## 实现
1. 后端新增 GET /api/posters/data?report_id={id}（鉴权）：
   一次装配海报全部数据——report（score/subscores/one_liner/date/
   workout_title）+ workout 摘要（movements_json 解析：力量动作每动作
   最佳组 weight×reps 按容量排序取 top3 亮点；有氧动作取 metrics 的
   distance/avgHeartRate 等摘要）+ 融合侧佳明数据（距离/平均心率）
   + PR 明细（复用 stats/ai 里现有 PR 检测逻辑，输出"动作名 重量×次数"）
   + 本周第 N 次训练计数 + workout_kind 判定（strength/cardio/mixed）；
   report 无 workout 关联时相应字段为 null；
2. utils/poster.js 重排 1080×1440 布局：
   - 有评分：评分环 + 标题同行；其下三子分横向条形（完成度/强度/恢复，
     各 0-100）；
   - 指标行按 workout_kind 适配：strength=容量/时长/热量；
     cardio=距离/时长/热量（有平均心率则四栏）；mixed 两行排列；
     任何形态禁止出现 "-" 占位；
   - PR 明细行（🏆 动作名 重量×次数，多条取第一）；
   - 亮点动作区 top2-3（动作名 + 最佳组；纯 cardio 无亮点则隐藏该区）；
   - one_liner 点评卡（自动换行，≤2 行截断）；
   - 无评分：隐藏评分环与子分条，标题上移，亮点区扩到 top4，
     指标行放大——禁止出现大片空白；
3. SharePosterButton：report.score 为 null 时按钮旁加提示
   「重新生成点评可解锁评分海报」（不阻断分享）；
4. 保持 Canvas 零依赖手绘、现代简约风不变。

## 测试要求（TDD 先红后绿）
- 后端装配端点：有/无 workout、有/无评分、top 亮点排序、PR 明细格式、
  本周计数、strength/cardio/mixed 判定与有氧摘要、401；
- poster.js：六种数据形态（完整力量/完整有氧/无评分/无 PR/无亮点/
  混合）绘制序列断言，重点断言无评分版布局紧凑（标题 y 坐标上移）、
  有氧版无 "-" 占位；
- 全量回归：backend pytest + frontend vitest + oxlint + build +
  test:capacitor；纯前后端改动，不用重装 APK。

完成后按 P0 第 9 条提交并输出完成报告，附各形态效果图。
```

---

## Sprint 9：数据通路扩展（任务 V3-7～）

**Sprint 目标**：扩展数据入口——常见运动文件格式导入（GPX/KML），后续任务待用户确认
（候选：身体数据围度四项 + 训记双向同步打通，即 C+ 方案）。

**背景锚点（2026-08-16 代码实地核对）**：
- 现有导入链路（V2-4）：`adapters/garmin_adapter.py` 的 `import_fit_file(session, path)`
  按后缀分发 `_parse_fit`（fitparse 库）/ `_parse_tcx`（stdlib xml.etree）→
  归一化 dict → sha256 去重 → 落 `garmin_activity` 表 → 调 `matcher.match_day`
  触发当日重匹配融合；
- API：`POST /api/import/fit`（`api/fit_import.py`），`ALLOWED_SUFFIXES = {.fit, .tcx}`，
  50MB 上限，空文件/未知后缀/解析失败均 422，接口全鉴权；
- 前端：`FitImportPage.jsx`（`ALLOWED_EXT = ['.fit', '.tcx']`，accept 属性，
  apiForm 上传，展示导入结果：日期/类型/匹配状态）；
- 测试基线：后端 671 passed / 6 skipped / 93.48%；前端 vitest 240 passed。

### V3-7 GPX/KML 运动文件导入

```
任务 V3-7：扩展文件导入，支持 GPX 与 KML 两种常见运动数据格式
（TCX 已支持，本次新增 GPX/KML，全链路复用现有导入管线）。

## 需求（用户原话）
"我需要一个导入运动数据的功能，包括 gpx, kml, tcx 等常见格式"
——GPX/KML 是 Strava/华为/咕咚/两步路等平台的通用导出格式，
用于补齐佳明生态外的历史与第三方运动记录。

## 实现约束（铁律相关）
1. 零新依赖：GPX/KML 都是 XML，一律用 stdlib xml.etree.ElementTree
   解析（_parse_tcx 已是此模式），禁止引入 gpxpy 等新包；
2. 不动 matcher/fuse/数据模型：解析器输出必须与 _parse_tcx 的归一化
   dict 同构（activity_name/activity_type/start_ts/duration_s/
   distance_m/calories/avg_hr/max_hr 等，字段缺失给 None），
   去重、落库、重匹配全部复用 import_fit_file 既有链路；
3. API 不新增端点：/api/import/fit 的 ALLOWED_SUFFIXES 扩为
   {.fit, .tcx, .gpx, .kml}（保持 URL 向后兼容），校验与限流不变。

## 解析要点
1. GPX（_parse_gpx）：
   - 命名空间兼容 1.1（http://www.topografix.com/gpx/1/1）与 1.0；
   - trkpt 取 lat/lon/ele/time（UTC ISO8601，时区处理与 _parse_tcx 一致）；
   - 心率扩展：gpxtpx:TrackPointExtension/gpxtpx:hr
     （ns http://www.garmin.com/xmlschemas/TrackPointExtension/v1），
     有则算 avg/max，无则 None；
   - 运动类型：trk 下 type/name 有则用，无则 'other'；
   - distance_m 无原始字段时用 haversine 逐点累加（math 模块自实现，
     与 ele 爬升无关，只算平面距离）；
2. KML（_parse_kml）：
   - 命名空间 kml 2.2（http://www.opengis.net/kml/2.2）+
     gx（http://www.google.com/kml/ext/2.2）；
   - 优先 gx:Track：`<when>` 与 `<gx:coord>lon lat ele</gx:coord>`
     一一对应解析；
   - 仅有 LineString `<coordinates>`（无时间戳）时无法确定运动日期，
     直接 422 并提示"该 KML 缺少时间信息，请导出含 gx:Track 的版本"；
   - KML 一般无心率/类型，相应字段 None、activity_type 默认 'other'；
3. 安全与健壮：XML 解析异常 → FitImportError（422 友好文案）；
   stdlib ET 默认不解析外部实体，无需额外 XXE 处理，但测试里
   放一条带 DOCTYPE 的恶意样本确认拒绝或不执行外部引用；
4. 前端：FitImportPage 的 ALLOWED_EXT/accept/提示文案扩为四种格式，
   导入结果展示逻辑不变；
5. 文档：PRD §6.2 的导入段落补充 GPX/KML 支持说明（含 KML 时间戳限制）。

## 测试要求（TDD 先红后绿）
- 最小样本全部内联写在测试里，不入库真实文件：
  GPX（带 gpxtpx 心率 / 无心率 / 1.0 命名空间）、
  KML（gx:Track 正常 / LineString 无时间 → 422）、
  损坏 XML → 422、空文件 → 422、未知后缀 → 422、
  带 DOCTYPE 外部实体样本 → 不泄露不执行；
- 归一化字段断言：start_ts/duration_s/distance_m（haversine 误差容忍
  ±5%）/avg_hr/max_hr/activity_type 默认值；
- 端到端：POST /api/import/fit 上传 GPX → garmin_activity 落库 →
  match_day 被调用（mock 断言）→ 重复上传同文件去重不新建行；
- 前端：FitImportPage 扩展名允许/拒绝用例更新；
- 全量回归：backend pytest（基线 671 passed）+ frontend vitest
  （基线 240 passed）+ oxlint + build + test:capacitor。

完成后按 P0 第 9 条提交并输出完成报告。
真机/实地验收（管理员做）：拿真实 GPX/KML 文件走一遍导入→匹配→
详情页展示链路。
```

### V3-8 AI 报告追问对话

```
任务 V3-8：每份 AI 报告支持追问对话——用户对报告内容有疑问时，
可就该报告与 AI 教练多轮对话，获得进一步解释或补充建议。

## 需求（用户原话）
"对于每个 AI 报告，都有一个追问沟通的能力。我觉得里面有些地方
不清楚，需要有能力跟他对话，让他进一步解释或者完善。"

## 背景锚点（2026-08-18 代码实地核对）
- adapters/llm.py 统一接口 chat(messages, model_override=None)
  原生接受消息列表，多轮对话零适配成本；strip_think 已有；
  compute_cost + token 记账模式见 services/ai.py（426-461 行）；
- AIReport 模型（models.py 158 行起）：type/workout_id/
  period_start/period_end/model/content_md/score 等；
- 现有报告 API：/api/ai-reports 的 list/get/generate/regenerate，
  全鉴权；异步 202+轮询模式有 ReviewGenerateManager 先例；
- 前端报告详情：AIReportsPage 卡片+详情（移动端 BottomSheet 抽屉 /
  桌面端分栏），ReviewContent 用 SimpleMarkdown 渲染。

## 实现
1. 数据模型（Alembic 迁移）：
   report_chat_message(id PK, report_id FK→ai_report.id
   ON DELETE CASCADE, role String(10) -- user/assistant,
   content Text, model String(100), prompt_tokens Int,
   completion_tokens Int, cost_estimate Float,
   client_request_id String(64) UNIQUE,  -- 幂等键
   created_at DateTime server_default now())；
2. services/report_chat.py：
   - 消息装配：system prompt = 教练人设 + 本报告全文
     （content_md 超 8000 字截断保留尾部结论）+ 报告元信息
     （类型/日期/评分）→ 历史消息（最近 20 条窗口）→ 新用户消息；
   - 调 LLMClient.chat（provider 用 settings 当前默认，与报告
     生成同一来源），assistant 回复过 strip_think；
   - token 记账与 compute_cost 复用 ai.py 模式，落库到消息行；
3. API（挂 /api/ai-reports，全鉴权）：
   - GET /api/ai-reports/{id}/messages → 该报告对话历史（按时间正序）；
   - POST /api/ai-reports/{id}/messages，body {content,
     client_request_id} → 落用户消息 → 调 LLM → 落 assistant 回复
     → 返回两条消息；
   - 同步返回（单次 chat 通常 5-30 秒，远低于 504 量级；假设：
     若实测频繁超时再改 202 轮询，本任务不做）；
   - 校验：报告不存在 404；content 空白 422；超 1000 字 422；
     单报告消息总数达 100 条 422（提示"对话过长"）；
   - 幂等：client_request_id 已存在时直接返回已落库的消息对，
     不得重复调 LLM；
4. 前端（报告详情内嵌，抽屉与桌面分栏同一组件）：
   - 报告正文下方新增「追问 AI 教练」区块：空态折叠入口；
   - 对话线程：用户消息右气泡、assistant 左气泡，
     assistant 内容用 SimpleMarkdown 渲染；
   - 输入框 + 发送按钮（回车发送、Shift+回车换行）；
     发送中 loading「教练思考中…」按钮禁用；失败可重试；
   - 移动端 BottomSheet 场景注意键盘弹起时输入框不被遮挡；
5. 成本护栏：单条 ≤1000 字 + 历史窗口 20 条 + 单报告 100 条上限，
   完成报告里附一次真实追问的 token/成本实测数字。

## 测试要求（TDD 先红后绿）
- 迁移 upgrade/downgrade 往返；级联删除（删报告→消息清空）；
- 服务层：消息装配顺序（system→历史→新消息）、20 条窗口裁剪、
  8000 字截断、strip_think 生效、token/成本落库、
  幂等重放不调 LLM（mock chat_fn 断言调用次数）；
- API：401/404/422 三态、POST→GET 历史闭环、并发重复
  client_request_id 只落一对；
- 前端：线程渲染/发送流转/loading/失败重试/空态折叠展开，
  抽屉与桌面两形态；
- 全量回归：backend pytest（基线 688 passed / 6 skipped /
  93.50%）+ frontend vitest（基线 241 passed）+ oxlint +
  build + test:capacitor。

完成后按 P0 第 9 条提交并输出完成报告，附一次真实追问的
对话截图与 token/成本实测。
```

### V3-8b 修复：发送按钮在 APP 内无响应（2026-08-18 真机确诊）

```
任务 V3-8b：修复追问发送按钮在安卓 APP 内点击无反应的问题。

## 根因（管理员已确诊，2026-08-18 真机截图）
ReportChatSection.jsx 的 handleSend 调用了 crypto.randomUUID()——
该 API 仅在安全上下文（HTTPS 或 localhost）可用；本项目 APP 与
浏览器均走 HTTP 明文（http://118.24.143.172:8080，无域名未备案），
WebView 中 crypto.randomUUID 为 undefined，点击时在 try/catch 之外
同步抛 TypeError：无 loading、无报错提示，表现为"按钮点不动"。
桌面开发与 playwright 截图都在 localhost（安全上下文），所以
测试全绿但真机必现。

## 修复要求
1. 新增 frontend/src/utils/uuid.js 导出 uuidv4()：
   - 优先 crypto.randomUUID()（存在时）；
   - 否则用 crypto.getRandomValues（非安全上下文也可用）手工构造
     v4 UUID（版本位/变体位正确）；
   - getRandomValues 也不可用时退 Math.random（幂等键用途可接受）；
2. ReportChatSection 改用 uuidv4()；全仓 grep 确认无其他
   crypto.randomUUID 直接使用点；
3. 测试（TDD 先红后绿）：
   - mock 删除 crypto.randomUUID 后点击发送，断言 POST 正常发出
     （复现真机环境的回归测试，这是本次的核心用例）；
   - uuidv4 三分支单测：randomUUID 路径 / getRandomValues 路径 /
     Math.random 兜底路径，输出符合 v4 格式；
   - 全量回归：vitest（基线 250 passed）+ oxlint + build +
     test:capacitor；
4. 顺手治理：docs/TECH_DEBT.md 新增一条经验登记——
   "HTTP 明文环境禁用安全上下文限定 API（如 crypto.randomUUID），
   前端新代码引入浏览器 API 前查 https://developer.mozilla.org
   的 Secure context 标注"。

完成后按 P0 第 9 条提交并输出完成报告。
真机验收（管理员做）：重装部署后 APP 内发送追问成功。
```

### V3-9 体脂秤报告图导入（PICOOC 全家桶指标）

```
任务 V3-9：体脂秤"身体测量报告"图片导入——拍照/上传体脂秤导出图，
视觉识别提取全部指标，用户确认后批量入库。

## 需求（用户原话）
"我的体脂秤可以导出这样的图片，我想通过图片导入的方式把身体数据
记录进去，包括图片中所有的数据。"
样本：有品 PICOOC 身体测量报告长图（已留档 backend 测试参考），
含日期时间、总分、体重/内脏脂肪指数/基础代谢率/脂肪率/
储肌能力等级/肌肉率/水分/蛋白质/骨量/BMI/身体年龄/身体类型。

## 背景锚点（2026-08-18 代码实地核对）
- 视觉链路已有成熟模板：services/screenshot.py（build_prompt /
  parse_json_content / validate_extraction / extract→confirm
  两步流 + feedback 重试），视觉调用固定走 adapters.llm 的
  vision_extract（Kimi 多模态，含 llm_call 记账）——本任务
  照此模式新写 body_image.py，不改 screenshot.py；
- body_metric 表：(date, type) 唯一约束幂等 upsert；
  类型白名单在 services/body_metrics.py 的 METRIC_TYPES
  （含单位与 min/max 区间校验），SYNCABLE_TYPES={weight, bodyfat}
  走训记三段式同步（V1-7 已有）；
- 前端：BodyMetricsPage 录入+趋势；ScreenshotImportPage 是
  extract→confirm 两步交互的现成参照。

## 实现
1. METRIC_TYPES 扩展 10 个新类型（含单位与合理区间校验）：
   visceral_fat 内脏脂肪指数(级 1-59) / bmr 基础代谢率(kcal
   500-5000) / muscle_rate 肌肉率(% 0-100) / water_rate 水分(%) /
   protein_rate 蛋白质(%) / bone_mass 骨量(kg 0.5-10) /
   muscle_ability 储肌能力等级(级 0-10) / bmi(10-80) /
   body_age 身体年龄(岁 1-120) / body_score 身体评分(分 0-100)；
   区间定宽一点（防误判拦截合法值），越界仅警告不拒绝；
2. services/body_image.py（仿 screenshot.py 结构）：
   - build_prompt：固定输出 schema body_scale_v1——
     {"schema":"body_scale_v1","date":"YYYY-MM-DD",
      "metrics":[{"type":"weight","value":86.7},...]}
     图片只有"8月18日"无年份时默认当前年；文本类（身体类型
     "偏胖型"）不入 metrics；
   - 指标名→type 映射表写死在 prompt 里（脂肪率→bodyfat、
     体重→weight、内脏脂肪指数→visceral_fat 等）；
   - parse_json_content 复用 screenshot 的同名函数
     （提取为共享或 import，禁止复制粘贴）；
   - validate：date 合法、metrics 每条 type 在 METRIC_TYPES、
     value 数值化；失败带 feedback 重试 1 次（复用现有模式）；
3. API（全鉴权）：
   - POST /api/body-metrics/extract-image（multipart 图片，
     ≤10MB，jpg/png）→ 返回识别结果（不落库）；
   - POST /api/body-metrics/confirm-import → 按 (date,type)
     upsert 批量入库；body 带每条指标的 selected 标记；
   - confirm 时若勾选了 sync_xunji 且类型在 SYNCABLE_TYPES，
     走既有训记三段式同步（复用，不重写）；
4. 前端：BodyMetricsPage 加「体脂秤图片导入」入口 →
   上传 → 识别结果确认页（日期可改 + 指标表格逐条可编辑数值/
   可勾选是否入库，默认全选 + "同步体重/体脂到训记"勾选项默认
   不勾）→ 入库成功 toast；趋势图类型切换器加入新指标
   （分组：基础=体重/体脂/BMI，成分=肌肉率/水分/蛋白质/骨量，
   评估=内脏脂肪/代谢/身体年龄/评分）；
5. 假设（记入 commit message）：身体年龄/评分/储肌等级这类
   "派生评估值"也入库（用户原话"所有数据"）；图片时间
   （08:18）不存，body_metric 按日幂等；身体类型文本丢弃。

## 测试要求（TDD 先红后绿）
- 服务层：prompt 含映射表与 schema、parse/validate 各分支
  （缺 date/非法 type/字符串数值/无年份日期默认当前年）、
  越界警告不拦截、feedback 重试 1 次、vision_extract mock；
- API：401/413(超限)/422(非图片/识别失败)、extract 不落库、
  confirm 幂等 upsert（同日同 type 重复导入覆盖不重复建行）、
  selected=false 不入库、sync_xunji 勾选调同步（mock）；
- 前端：上传→确认页渲染/编辑/勾选→入库流转，新指标趋势切换；
- 全量回归：backend pytest（基线 709 passed / 6 skipped /
  93.43%）+ frontend vitest（基线 256 passed）+ oxlint +
  build + test:capacitor；
- 真实样本验收：用用户的 PICOOC 样图跑一次 extract，
  11 项数值与图片逐项核对一致（完成报告附对照表）。

完成后按 P0 第 9 条提交并输出完成报告，附真实样本识别对照表。
真机验收（管理员做）：APP 内拍/传体脂秤图走完整导入链路。
```

### V3-10 移动端 UI 修复（日历头部 + 报告内嵌图表，2026-08-19 真机确诊）

```
任务 V3-10：修复两处移动端 UI bug（真机截图确诊，见下）。

## Bug 1：日历页头部移动端挤压重叠
- 现象（CalendarPage）：一行 flex justify-between 塞了
  [←上个月][2026-08][SyncButton 按钮+同步状态文案][下个月→]，
  手机上月份标题折成两行（"2026-"/"08"）、按钮文字竖排、
  同步状态文案与月份标题视觉重叠；
- 修复（CalendarPage.jsx 60-83 行区域）：
  - 移动端改为两行布局：第一行 [←上个月][月份][下个月→]，
    月份 h2 加 whitespace-nowrap shrink-0 禁止折行；
    第二行 SyncButton 独占一行（按钮+状态文案自然排列）；
  - 桌面端（md: 起）保持现有单行布局零变化；
  - 用 Tailwind 响应式 class 实现（flex-col md:flex-row 等），
    不需要 JS 判断；

## Bug 2：报告内嵌 ECharts 图表移动端文字重叠
- 现象（ReviewContent 的 EChartBlock）：AI 生成的 ```echarts
  option 按桌面宽度假设，窄屏下长标题与 Y 轴名重叠、
  柱顶数值标签与标题碰撞、图例挤压；
- 根因：EChartBlock 把 LLM option 原样 setOption，无任何
  移动端适配；V3-3 的 applyMobile 只覆盖了趋势页图表；
- 修复（ReviewContent.jsx EChartBlock）：
  - 用 useIsMobile() 判断，移动端对解析后的 option 做深合并
    覆盖（不动 LLM 原文，只在渲染层修正）：
    title: { left:'center', top:0, textStyle:{ fontSize:13 } }；
    grid: { top:56, left:8, right:8, bottom:44, containLabel:true }；
    yAxis.name 保留但 nameTextStyle.fontSize=10；
    xAxis.axisLabel: { fontSize:10 }（类目轴 rotate:30）；
    legend: { bottom:0, textStyle:{ fontSize:10 }, type:'scroll' }；
    series[].label.fontSize=9；
  - 深合并写一个小的 mergeMobileOption(option) 纯函数
    （与 V3-3 applyMobile 同思路，浅层数组合并即可），
    桌面端不合并、option 原样渲染零变化；
  - 图表高度 h-64 不变；

## 测试要求（TDD 先红后绿）
- mergeMobileOption：title/grid/legend/axisLabel/series label
  覆盖断言；无对应键时安全合并（如无 legend 则新建）；
  输入 option 不被修改（不可变性）；
- EChartBlock：移动端渲染调用 setOption 的参数含合并后配置
  （mock echarts.init），桌面端原样；
- CalendarPage：移动端 class 断言（月份标题 whitespace-nowrap、
  SyncButton 独立行），桌面端 class 不变；
- 全量回归：vitest（基线 263 passed）+ oxlint + build +
  test:capacitor。纯前端改动，后端不动。

完成后按 P0 第 9 条提交并输出完成报告。
真机验收（管理员做）：日历月切换+同步中状态显示、周报抽屉内
图表标题/轴名/标签互不重叠。
```

---

## 附：敏捷纪律速查（贴在每个 Sprint 开始前给自己看）

- **一个任务一个会话**：上下文污染是 vibe coding 质量下降的主因；
- **测试不绿不进下一任务**，没有例外；
- **AI 跑偏时**：先让它重读 PRD 对应章节再继续，而不是口头纠正（口头纠正在长上下文中会丢失）；
- **每 Sprint 结束必做复盘**，技术债落文档，防止积重难返；
- **M7 真机验证是硬门槛**：匹配准确率不达标宁可加一周也不进 Sprint 3。

---

## 附：GitHub Actions CI 配置任务（运维增强，可随时执行）

```
任务 CI-1：为仓库 https://github.com/fmmf3537/fitness-app 配置 GitHub Actions CI，
每次 push 到 main 和 PR 自动跑全部测试。

1. 新建 .github/workflows/ci.yml，两个 job：
   - backend：ubuntu-latest + Python 3.11；pip install -r backend/requirements.txt 后
     cd backend && python -m pytest tests/ -q（pytest.ini 已内置 85% 覆盖率门禁，低于即红）；
   - frontend：ubuntu-latest + Node 20；cd frontend && npm ci 后依次跑
     npx vitest run（注意：若 package.json 的 test 脚本是 watch 模式，CI 必须显式用 run 模式防挂起）、
     npm run build、npx oxlint src；
2. 关键约束——CI 无 .env、无任何 Secrets：
   - 全部集成测试已由 RUN_XUNJI_INTEGRATION / RUN_GARMIN_INTEGRATION / RUN_LLM_INTEGRATION /
     RUN_VISION_INTEGRATION 显式门禁，CI 中应全部跳过（预期 4 skipped），绝不允许真实外呼；
   - 先在本地验证：临时把 .env 移走（或开无环境变量的干净 shell）跑全量测试必须全绿，
     若有用例隐式依赖 .env（如 FERNET_KEY），修复为测试内自构造后再提交；
3. README.md 顶部加 CI 状态徽章（actions/workflows/ci.yml/badge.svg）；
4. 提交推送到 main 后，在 GitHub Actions 页确认首次运行两个 job 全绿，
   把运行结果截图或日志摘要附在完成报告里。

TDD 要求：本任务为基建配置，以"本地无 .env 全绿 + 首次 CI 运行全绿"为验收标准。
完成后按 P0 第 9 条提交并输出完成报告。
```
