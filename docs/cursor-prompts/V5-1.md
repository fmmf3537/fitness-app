# V5-1 切片开发提示词：AI 教练长期记忆 — 后端表结构 + CRUD API + 记忆段组装函数

> 你是本仓库的资深后端工程师（FastAPI + SQLAlchemy 2.x + Alembic + pytest）。本提示词是唯一任务来源：只按本提示词写代码，**不跑任何验收命令（pytest 一律不跑）**，**不执行任何 git 命令**。
> 红线：禁整文件重写（外科式最小修改）；所有写出的文件必须无 BOM；不得修改 `.env` / `.env.*` / `.env.example` / `.env.production.example`；不得删除任何既有文件；不得改动本提示词文件预算之外的任何文件。

## 需求（PRD-V5 第 5/6/7 节，V5-1 范围）

为 AI 教练长期记忆实现**数据层**：4 张新表 + 1 个 settings 扩展 + CRUD API + 记忆段组装纯函数。
V5-2（提炼任务）与 V5-3（L3 统计）**不在本切片**，本片只做存储与读写。

## 已核实的关键事实（实读代码，可直接采信）

### 迁移链（alembic）

- 当前 head：`b7c8d9e0f1a2`（`backend/alembic/versions/b7c8d9e0f1a2_workout_set_hr_table.py`，V4-7）
- 新迁移 `down_revision = "b7c8d9e0f1a2"`，revision 用 **`c8d9e0f1a2b3`**
- 迁移文件风格（见 b7c8d9e0f1a2）：
  - `op.create_table(...)` 手写列 + `sa.UniqueConstraint(..., name="uq_...")`
  - `created_at` 用 `sa.DateTime(), server_default=sa.func.now(), nullable=False`
  - downgrade 只 `op.drop_table(...)`

### 模型风格（backend/app/models.py）

- SQLAlchemy 2.x `Mapped` / `mapped_column`
- `__tablename__` 单数；幂等约束 `UniqueConstraint(..., name="uq_...")`
- `server_default=func.now()` 作 created_at 默认
- 新模型追加到 `models.py` 文件末尾既有模型之后（WorkoutSetHr 之后，当前文件 308 行）

### API 风格（backend/app/api/）

- `router = APIRouter(prefix="/api/xxx", tags=["xxx"], dependencies=[Depends(require_auth)])`
- `from app.api.auth import require_auth`
- Pydantic `BaseModel` 定义请求体，校验失败走 FastAPI 422
- 薄封装：业务逻辑在 `services/` 纯函数，api 层只调服务 + 转 HTTPException

### 测试风格

- `backend/tests/test_alembic.py`：`EXPECTED_TABLES` 集合（第 10-25 行附近）需加入新表名；roundtrip 测试参照 `test_skinfold_and_settings_profile_roundtrip` 模式，**一律用显式 revision 回退**（`command.downgrade(cfg, "b7c8d9e0f1a2")`），严禁相对回退
- 服务/API 测试参照 `backend/tests/test_body_metrics.py` 与 `backend/tests/test_api_workouts.py` 的 client/auth fixture 模式（conftest.py 已提供 `client` 与 `auth_user` fixture）

### 现有相关代码

- `backend/app/api/auth.py`：`require_auth(authorization)` 返回 token 字符串（39 行，单用户内存会话）
- `backend/app/db.py`：`get_session` 依赖
- `backend/app/models.py`：已存在 `Setting`（含 `gender`/`birth_date`，V4-3）、`WorkoutSetHr` 等 13 张表
- 无任何现成的 "偏好 / 记忆 / 聊天" 表（已 grep 确认：`coach_` 前缀表不存在）

## 文件预算（共 9 个，不得越界）

新 5：
1. 新 `backend/alembic/versions/c8d9e0f1a2b3_coach_memory_tables.py`
2. 新 `backend/app/services/coach_memory.py`（CRUD + build_memory_section）
3. 新 `backend/app/api/coach_memory.py`（偏好/草稿 CRUD API）
4. 新 `backend/tests/test_coach_memory.py`（服务层 + API 单测）
5. 新 `backend/tests/test_coach_memory_alembic.py`（迁移 roundtrip）

改 4：
6. 改 `backend/app/models.py`（追加 4 个模型，挂在 WorkoutSetHr 之后）
7. 改 `backend/app/main.py`（注册 coach_memory_router，一行）
8. 改 `backend/tests/test_alembic.py`（EXPECTED_TABLES 加 4 个表名）
9. 改 `backend/app/models.py` 的 Setting（追加 `memory_default_provider` 列）——与 6 合并为同一文件改动

## 开发内容

### 1. 模型（models.py 追加 4 个，尾部）

```python
class CoachPreference(Base):
    """教练须知（L1 正式清单，手动维护 + AI 采纳）。"""

    __tablename__ = "coach_preference"

    id: Mapped[int] = mapped_column(primary_key=True)
    content: Mapped[str] = mapped_column(Text)
    category: Mapped[str | None] = mapped_column(String(30))  # manual / ai_suggested
    tags: Mapped[str | None] = mapped_column(String(200))     # 逗号分隔
    source: Mapped[str | None] = mapped_column(String(30))    # user / weekly_review / daily_distill
    active: Mapped[bool] = mapped_column(Boolean, default=True, server_default="1")
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())


class CoachPreferenceDraft(Base):
    """AI 提炼草稿（L1 待确认，D1）。"""

    __tablename__ = "coach_preference_draft"

    id: Mapped[int] = mapped_column(primary_key=True)
    content: Mapped[str] = mapped_column(Text)
    tags: Mapped[str | None] = mapped_column(String(200))
    source: Mapped[str | None] = mapped_column(String(30))   # sys_prompt / daily_distill / weekly_review
    status: Mapped[str] = mapped_column(String(20), default="pending")  # pending / accepted / rejected / merged
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    resolved_at: Mapped[datetime | None] = mapped_column(DateTime)


class CoachMemory(Base):
    """对话记忆摘要（L2）。"""

    __tablename__ = "coach_memory"

    id: Mapped[int] = mapped_column(primary_key=True)
    summary: Mapped[str] = mapped_column(Text)
    tags: Mapped[str | None] = mapped_column(String(200))
    source: Mapped[str | None] = mapped_column(String(20))   # report_chat / coach_chat
    ref_report_id: Mapped[int | None] = mapped_column(Integer)
    ref_chat_id: Mapped[int | None] = mapped_column(Integer)
    active: Mapped[bool] = mapped_column(Boolean, default=True, server_default="1")
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())


class CoachChatMessage(Base):
    """独立聊天消息（D7 持久对话）。"""

    __tablename__ = "coach_chat_message"
    __table_args__ = (
        UniqueConstraint("client_request_id", name="uq_coach_chat_message_crid"),
    )

    id: Mapped[int] = mapped_column(primary_key=True)
    role: Mapped[str] = mapped_column(String(10))      # user / assistant
    content: Mapped[str] = mapped_column(Text)
    model: Mapped[str | None] = mapped_column(String(100))
    prompt_tokens: Mapped[int | None] = mapped_column(Integer)
    completion_tokens: Mapped[int | None] = mapped_column(Integer)
    cost_estimate: Mapped[float | None] = mapped_column(Float)
    client_request_id: Mapped[str | None] = mapped_column(String(64))
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
```

### 2. Setting 扩展（models.py Setting 类内）

```python
# V5：记忆提炼默认 provider（空 = 用当前默认 LLM）
memory_default_provider: Mapped[str | None] = mapped_column(String(50))
```

### 3. 迁移（c8d9e0f1a2b3）

- `revision = "c8d9e0f1a2b3"`，`down_revision = "b7c8d9e0f1a2"`
- upgrade：`op.create_table` × 4（coach_preference / coach_preference_draft / coach_memory / coach_chat_message），列与模型一一对应（Integer/Float 注意 import sa.Float；Text 用 sa.Text）
- **注意**：`coach_preference` / `coach_memory` 的 `updated_at` 列（coach_preference 有，coach_memory 没有）与 `server_default=func.now()` 一并写
- **单独处理 settings 扩展**：`op.add_column("settings", sa.Column("memory_default_provider", sa.String(length=50), nullable=True))`（SQLite 可加可空列，无需 batch）
- downgrade：drop 4 表 + drop settings 列

### 4. 服务 `services/coach_memory.py`

纯函数与 IO 分离，至少导出：

- `list_preferences(session, active_only=True) -> list[CoachPreference]`（按 updated_at 倒序）
- `create_preference(session, *, content, category="manual", tags=None, source="user") -> CoachPreference`（content 去重：已存在相同 content 且 active 时返回既有行）
- `update_preference(session, pref_id, *, content=None, tags=None) -> CoachPreference`（不存在抛 ValueError）
- `deactivate_preference(session, pref_id) -> None`（软删 active=False，不物理删除）
- `list_drafts(session, statuses=None) -> list[CoachPreferenceDraft]`（默认只 pending，按 created_at 倒序）
- `accept_draft(session, draft_id) -> CoachPreference`（draft.status='accepted' + resolved_at=now；复制到 coach_preference（category='ai_suggested', source=draft.source）并 content 去重：若已存在相同 content 的 active 条目则返回既有行不再新增；重复时 draft.status='merged'）
- `reject_draft(session, draft_id) -> None`（status='rejected' + resolved_at=now）
- `search_memory(session, tags: list[str], limit: int = 5) -> list[CoachMemory]`
  （D5 V1：对每条 active memory，若其 tags 与传入的任一 tag 相同/包含则命中；按 created_at 倒序取 limit 条；无命中时返回最近 limit 条任意 active 记忆作兜底）
- `build_memory_section(l1: list[str], l2: list[str], l3: dict | None = None) -> str`
  **纯函数**：返回注入 prompt 的 Markdown 段：
  ```
  ## 用户长期记忆（AI 参考）
  须知：
  - 深蹲时注意右膝旧伤...
  历史对话要点：
  - (最近) ...
  长期统计：
  - 训练频率...
  ```
  - l1 为空 → 须知段省略；l2 为空 → 历史段省略；l3 为空/None → 统计段省略
  - 三段落都存在时输出为上述三节；部分为空时只输出非空节
  - 全部为空 → 返回空字符串 `""`（不输出任何内容，AC5：无记忆数据时该段不出现）

### 5. API `api/coach_memory.py`

```
router = APIRouter(prefix="/api/coach/preferences", tags=["coach"], dependencies=[Depends(require_auth)])
- GET  ""                       → {"preferences": [...]}  （active_only 默认 True）
- POST ""                       → 新建（body: {content, tags?}，category 固定 manual）
- PUT  "/{id}"                  → 改 content/tags
- DELETE "/{id}"                → 软删（返回 {"ok": True}）

router2 = APIRouter(prefix="/api/coach/drafts", tags=["coach"], dependencies=[Depends(require_auth)])
- GET  ""                       → {"drafts": [...]}（默认 pending）
- POST "/{id}/accept"           → {"preference": {...}}
- POST "/{id}/reject"           → {"ok": True}
```

- 均走 `session: Session = Depends(get_session)`
- 服务层抛 `ValueError` → `HTTPException(400, detail=str(exc))`
- 返回 dict 中条目用 `to_dict()`（在服务层实现，返回 id/content/category/tags/source/active/created_at/updated_at）

### 6. main.py 注册（一行）

```python
from app.api.coach_memory import router as coach_memory_router
app.include_router(coach_memory_router)
```

### 7. 测试

**`tests/test_coach_memory.py`**（服务层 + API，全离线）：

1. create_preference：新建 + 相同 content 幂等返回既有行；tags 保存
2. update_preference：改 content/tags；不存在抛 ValueError
3. deactivate_preference：active=False；list(active_only=True) 不再返回
4. accept_draft：新建草稿 → accept → 产出 coach_preference(category='ai_suggested')；重复 accept 同 content → 不新增（status='merged'）
5. reject_draft：status='rejected'；resolved_at 落
6. search_memory：命中 tag + 兜底（无 tag 命中返回最近 active）；limit 截断
7. build_memory_section：三段都在 / 只 l1 / 只 l2 / 只 l3 / 全空返回 ""（AC5）
8. API：POST 新建 + GET 列表 + PUT 改 + DELETE 软删 + drafts accept/reject 全链路（用 client + auth_user fixture）

**`tests/test_coach_memory_alembic.py`** 或并入 `test_alembic.py`：

- `EXPECTED_TABLES` 加 `coach_preference` / `coach_preference_draft` / `coach_memory` / `coach_chat_message`
- roundtrip：upgrade head 后 4 表存在 + settings.memory_default_provider 列存在；`command.downgrade(cfg, "b7c8d9e0f1a2")` 后 4 表消失 + settings 列消失；再 upgrade head 恢复。**严禁 `downgrade -1` 相对回退**

## 自报告要求（交付时必须给出）

1. 改动文件清单（对照预算 9 个逐一列出，注明 新/改）。
2. 每个既有文件（models.py / main.py / test_alembic.py）的改动行数与位置说明。
3. 新增测试用例数量与名称列表。
4. 设计取舍说明（如有偏离本提示词之处，必须显式列出并说明理由）。
5. 确认：未跑 pytest、未执行 git、文件无 BOM、未碰 .env 系列。
6. 行为自检清单：
   - [ ] 4 张新表 + settings 扩展已在迁移中
   - [ ] down_revision = "b7c8d9e0f1a2"
   - [ ] build_memory_section 全空返回 ""
   - [ ] search_memory 有兜底逻辑
   - [ ] accept_draft 有 content 去重 + merged 状态
   - [ ] API 全部依赖 require_auth
   - [ ] main.py 只增加 2 行（import + include_router）