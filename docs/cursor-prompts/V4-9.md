# V4-9 切片开发提示词：逐组心率存量回填 + session_review prompt 注入（F4 收尾）

> 你是本仓库的资深后端工程师（FastAPI + SQLAlchemy 2.x + pytest）。本提示词是唯一任务来源：只按本提示词写代码，**不跑任何验收命令（pytest 一律不跑）**，**不执行任何 git 命令**。
> 红线：禁整文件重写（外科式最小修改）；所有写出的文件必须无 BOM；不得修改 `.env` / `.env.*` / `.env.example` / `.env.production.example`；不得删除任何既有文件；不得改动本提示词文件预算之外的任何文件。

## 需求（PRD-V4 F4 收尾，开放问题已确认）

1. **存量回填脚本**（Q4.2 已确认）：对存量 workout 全量回填逐组心率——独立手动触发脚本，
   **不进每日同步链路**；幂等可重跑。
2. **点评注入**（PRD F4 方案 4）：session_review prompt 增加「逐组心率」摘要段
   （每动作：组心率均值/峰值/组后 30s 恢复序列），供 AI 分析组间强度衰减与恢复；
   无逐组心率数据的 workout prompt 保持原样。

## 已核实的关键事实（实读代码，可直接采信）

### V4-7 已交付（commit 6037b1b）

- `backend/app/services/set_hr.py`：
  - `compute_workout_set_hr(session, workout) -> list[WorkoutSetHr]`——**幂等**（先删该
    workout 全部旧行再插入）；无 garmin 关联/无 movements_json/无 exercise_sets/无心率序列
    → 返回 `[]` 且**不删旧行**（前置校验在 delete 之前）。
  - `WorkoutSetHr` 模型字段：workout_id / movement_name / set_index（动作内原始 1-based）/
    hr_avg / hr_max / hr_min / hr_recovery_30s（可空）/ set_start / set_end /
    confidence（high/low）/ match_method / created_at。
- `backend/app/db.py`：`make_engine(url=None)`、`make_session_factory(engine)` —
  脚本引导用；`get_settings().database_url` 自动解析。

### ai.py 现状（V4-1/V4-2/V4-5 已大改，2236 行，严禁整文件重写）

- `build_session_review_prompt(workout: dict, history, recovery, activity_history=None, *,
  feedback=None)`（第 398 行）：纯函数，返回 `[{"role":"system",...},{"role":"user",...}]`。
  - 「## 动作组次」段在第 429-473 行（`else: lines.append("- 无动作数据")` 结束）；
    第 475 行起为历史段。**注入点：动作组次段之后、历史段之前**。
  - 既有惯例（V4-5 feedback）：**可选参数为空/None 时输出与原版逐字节一致**——
    本切片的 set_hr 参数必须遵守同一惯例。
- `generate_session_review(session, workout_id, *, chat_fn=None, feedback=None, purpose=None)`
  （第 624 行）：第 644 行 `movements = _parse_movements(workout)`；第 681-684 行调 builder。
  `regenerate_session_review_with_feedback`（第 910 行）与 `run_daily_reviews` 都路由到
  本函数——**在本函数注入即全覆盖，不需改它们**。
- 既有 fake chat_fn 测试模式（`backend/tests/test_ai.py:205-212`）：返回
  `{"content": "## 完成质量\n不错\n## 与历史对比\n持平\n```json\n{\"schema\":\"session_review_v1\",...}\n```",
  "prompt_tokens": 100, "completion_tokens": 20}`（正文须附 session_review_v1 JSON 块，
  否则触发重试/降级）。
- conftest helpers：`make_xunji_train` / `make_garmin_activity` / `fuse_workout`
  （`backend/tests/conftest.py`）；软删除用 `app.services.workouts.delete_workout`。

## 文件预算（共 3 个，不得越界；对应清单「新 1 + 改 1 + 测试 1」）

1. 新 `backend/scripts/backfill_set_hr.py`
2. 改 `backend/app/services/ai.py`（仅两处：builder 加参+注入段；generate_session_review 查询+透传）
3. 新 `backend/tests/test_set_hr_backfill.py`

## 开发内容

### 1. `backend/scripts/backfill_set_hr.py`（回填脚本）

结构：**核心逻辑做成可导入函数**（供测试直接调），`main()` 只做引导：

```python
"""V4-9 存量逐组心率全量回填（手动触发，幂等可重跑，不进每日同步链路）。

用法：cd backend && ./.venv/Scripts/python.exe scripts/backfill_set_hr.py
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))  # backend/

from app.db import make_engine, make_session_factory
from app.models import Workout
from app.services.set_hr import compute_workout_set_hr


def run_backfill(session, *, log=print) -> dict:
    """对全部「未软删 + 训记佳明双关联」workout 逐个重算逐组心率。

    幂等：compute_workout_set_hr 内部先删后插。单 workout 异常不影响整体
    （rollback 后继续）。返回统计 dict。
    """
    stats = {"scanned": 0, "with_rows": 0, "rows": 0, "no_data": 0, "failed": 0}
    workouts = (
        session.query(Workout)
        .filter(
            Workout.deleted_at.is_(None),
            Workout.xunji_train_id.isnot(None),
            Workout.garmin_activity_id.isnot(None),
        )
        .order_by(Workout.date, Workout.id)
        .all()
    )
    for w in workouts:
        stats["scanned"] += 1
        try:
            rows = compute_workout_set_hr(session, w)
        except Exception as exc:  # 单点失败不阻断全量
            session.rollback()
            stats["failed"] += 1
            log(f"[failed] workout {w.id} {w.date}: {exc!r}")
            continue
        if rows:
            stats["with_rows"] += 1
            stats["rows"] += len(rows)
            log(f"[ok] workout {w.id} {w.date} {w.title or ''}: {len(rows)} 组")
        else:
            stats["no_data"] += 1
    log(f"回填完成：{stats}")
    return stats


def main() -> int:
    engine = make_engine()
    session = make_session_factory(engine)()
    try:
        run_backfill(session)
    finally:
        session.close()
        engine.dispose()
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

（允许微调格式，但 run_backfill 签名/统计键/幂等语义/失败继续 不得变。）

### 2. `backend/app/services/ai.py`（两处外科式修改）

**改动 A — builder 加 keyword-only 参数与注入段**：

- 签名改为 `*, feedback: list[str] | None = None, set_hr: list[dict] | None = None`。
  每个 dict 形状：`{"movement_name", "set_index", "hr_avg", "hr_max", "hr_min",
  "hr_recovery_30s", "confidence"}`。
- 在「## 动作组次」段结束之后（`else: lines.append("- 无动作数据")` 之后、
  第 475 行历史段空行之前）插入：

```python
    # V4-9 F4：逐组心率摘要段（有数据才注入；None/空 → 输出与原版逐字节一致）
    if set_hr and movements:
        by_movement: dict[str, list[dict]] = {}
        for r in set_hr:
            by_movement.setdefault(r.get("movement_name"), []).append(r)
        seg: list[str] = []
        for mv in movements:
            rows = by_movement.get(mv.get("name"))
            if not rows:
                continue
            rows = sorted(rows, key=lambda r: r.get("set_index") or 0)

            def _seq(key, _rows=rows):
                return " → ".join(
                    str(r.get(key)) if r.get(key) is not None else "-" for r in _rows
                )

            seg.append(f"- {mv.get('name')}：")
            seg.append(f"  - 组中心率均值：{_seq('hr_avg')} bpm")
            seg.append(f"  - 组中心率峰值：{_seq('hr_max')} bpm")
            if any(r.get("hr_recovery_30s") is not None for r in rows):
                seg.append(f"  - 组后30秒恢复心率：{_seq('hr_recovery_30s')} bpm")
            low = [r.get("set_index") for r in rows if r.get("confidence") == "low"]
            if low:
                seg.append(
                    f"  - 注：第 {'、'.join(str(i) for i in low)} 组为低置信匹配"
                    "（佳明动作识别与训记不一致），数据仅供参考"
                )
        if seg:
            lines.append("")
            lines.append("## 逐组心率（佳明实测，供分析组间强度变化与恢复）")
            lines.extend(seg)
```

- **不改 system 提示词**、不改 docstring 以外任何既有行；docstring 可补一句 set_hr 说明。

**改动 B — generate_session_review 查询并透传**（第 664 行 recovery 查询之后、
第 681 行 builder 调用处）：

```python
    # V4-9 F4：查询逐组心率注入 prompt（仅有动作的训练；无行则不传，prompt 保持原样）
    set_hr_payload: list[dict] | None = None
    if movements:
        set_hr_rows = (
            session.query(WorkoutSetHr)
            .filter(WorkoutSetHr.workout_id == workout_id)
            .order_by(WorkoutSetHr.movement_name, WorkoutSetHr.set_index)
            .all()
        )
        if set_hr_rows:
            set_hr_payload = [
                {
                    "movement_name": r.movement_name,
                    "set_index": r.set_index,
                    "hr_avg": r.hr_avg,
                    "hr_max": r.hr_max,
                    "hr_min": r.hr_min,
                    "hr_recovery_30s": r.hr_recovery_30s,
                    "confidence": r.confidence,
                }
                for r in set_hr_rows
            ]
```

builder 调用加 `set_hr=set_hr_payload`；`WorkoutSetHr` 加入文件顶部既有
`from app.models import ...` 列表（不改其他 import）。

### 3. `backend/tests/test_set_hr_backfill.py`（新测试文件）

conftest helpers + fuse_workout 构造数据；佳明 raw_json 手工构造小型 details+exercise_sets
（参照 `backend/tests/test_api_workout_set_hr.py` 的既有夹具模式，**不要 import 其他
测试模块的私有函数**）。至少 6 个用例：

1. `test_run_backfill_writes_rows_and_is_idempotent`：1 个 auto_matched workout（完整
   佳明数据，2 组）→ 第一遍 `stats["with_rows"] == 1`、`stats["rows"] == 2`；第二遍
   数据库行数不变、stats 相同。
2. `test_run_backfill_skips_unmatched_and_deleted`：xunji_only + garmin_only + 已软删
   的 matched workout → `scanned == 0` 或仅统计到未删 matched；这些 workout 不产生行。
3. `test_run_backfill_no_exercise_sets_counts_no_data`：matched 但佳明无 exercise_sets
   → `no_data == 1`、不建行。
4. `test_builder_injects_set_hr_section`：直接调 build_session_review_prompt（最小
   workout dict + 2 组 set_hr，含 1 个 low）→ user 内容含「## 逐组心率」、
   「100 → 105」式均值序列、低置信提示行。
5. `test_builder_without_set_hr_unchanged`：不传 set_hr vs `set_hr=None` 输出完全相等，
   且不含「逐组心率」。
6. `test_generate_session_review_injects_when_rows_exist`：fuse workout + 落
   WorkoutSetHr 行 + fake chat_fn 捕获 messages → 末条 user 内容含「## 逐组心率」；
   fake 返回合法 session_review_v1 块（参照 test_ai.py:205-212）。
7. `test_generate_session_review_no_rows_no_section`：无 WorkoutSetHr 行 → 不含
   「逐组心率」。

## 自报告要求（交付时必须给出）

1. 改动文件清单（对照预算 3 个逐一列出，注明 新/改）。
2. ai.py 两处改动的行号区间与行数；确认 feedback/exetype/activity_history 等既有逻辑未动。
3. 新增测试用例名称列表。
4. 确认：未跑 pytest、未执行 git、文件无 BOM、未碰 .env 系列、未改预算外文件。
