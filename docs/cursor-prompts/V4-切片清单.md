# PRD-V4 切片清单（2026-09-02）

> 接力跑切片用。规则：每片走完整 6 阶段（起草→启动→轮询→审核→修订→提交），通过后才起草下一片。
> 状态列：⬜ 未开始 / 🔵 进行中 / ✅ 已完成。
> 验收基线（2026-09-02 实测）：后端 784 passed / 6 skipped / 覆盖率 93.28%（门禁 85%）；前端 289 passed / 36 文件；build 通过。
> 验收命令：
> - 后端：`cd backend && ./.venv/Scripts/python.exe -m pytest -q --no-header -p no:cacheprovider`
> - 前端：`cd frontend && npx vitest run && npx vite build`

| 状态 | 切片ID | 对应 PRD | 内容 | 范围 | 预估文件预算 | 依赖 |
|---|---|---|---|---|---|---|
| ✅ | V4-1 | F1 | 同类活动历史修复：`query_activity_history`（近4周汇总+最近3次明细）+ garmin_only 点评 prompt 注入 + 活动类型中文映射 | 纯后端 | 改 2（ai.py、新 mapping 可并入 ai.py）+ 测试 1-2 | — |
| ✅ | V4-2 | F2 | exetype 语义渲染（辅助/负重）+ 有效负荷折算（取训练日最近体重，缺失降级）+ `query_movement_history` 按 exetype 分组统计 | 纯后端 | 改 1（ai.py）+ 测试 1-2 | V4-1（同文件顺序执行） |
| ✅ | V4-3 | F5 后端 | `skinfold_record` 表 + alembic 迁移 + 4 方案公式（文献样例校验）+ settings 加性别/出生日期 + 测量 API（同日同 method 幂等 upsert，结果写 body_metric bodyfat） | 纯后端 | 新 3-4 + 改 3 + 测试 2 | — |
| ✅ | V4-4 | F5 前端 | 设置页性别/出生日期表单 + 身体数据页「皮脂钳测量」面板（方案选择、JP3 按性别置顶、自测标注、上次值回显） | 纯前端 | 新 1 + 改 2 + 测试 1-2 | V4-3 |
| ✅ | V4-5 | F3 后端 | `POST /api/ai/session_review/{workout_id}/regenerate_with_feedback`（最近对话注入"用户反馈"段、日限 5 次、删旧重生成）+ next_advice 同机制端点 | 纯后端 | 改 2-3 + 测试 1-2 | V4-1、V4-2（重生成走新 prompt） |
| ✅ | V4-6 | F3 前端 | 详情页 SessionReviewSection 内嵌 ReportChatSection + 「根据以上讨论重新生成」按钮（覆盖确认、防抖）+ 下次建议重生成按钮（有条件显示）+ 海报预览刷新 | 纯前端 | 改 3-4 + 测试 1-2 | V4-5 |
| ✅ | V4-7 | F4 后端 | `workout_set_hr` 表 + 迁移 + `services/set_hr.py`（HR 序列提取、顺序对齐+佳明类别映射校验、组中均值/峰值+组后30s恢复、置信度分级）+ 详情 API 扩展 | 纯后端 | 新 2 + 改 3 + 测试 2 | — |
| ⬜ | V4-8 | F4 前端 | 详情页组次表心率列（均值/峰值/恢复，低置信度 ~ 前缀，无数据降级不显示） | 纯前端 | 改 2 + 测试 1 | V4-7 |
| ⬜ | V4-9 | F4 回填 | 存量全量回填脚本（手动触发、幂等可重跑）+ 逐组心率摘要注入 session_review prompt | 纯后端 | 新 1 + 改 1 + 测试 1 | V4-7 |
| ⬜ | V4-10 | 联调 | 全量回归收尾：后端 pytest + 前端 vitest + build 全绿，覆盖率门禁维持；TECH_DEBT/手册同步 | 文档+回归 | 改文档 1-2 | 全部 |

## 执行顺序

V4-1 → V4-2 → V4-3 → V4-4 → V4-5 → V4-6 → V4-7 → V4-8 → V4-9 → V4-10

（V4-3/V4-4 与 V4-7/V4-8 模块独立，理论上可提前，但接力串行跑时按上表顺序即可）

## 执行器

主力 opencode（已装已认证），备选 Codex（已配 MiniMax）。每片首跑预计 20-40 分钟，修订轮 4-5 分钟。
