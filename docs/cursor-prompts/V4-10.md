# V4-10 切片开发提示词：联调收尾——TECH_DEBT 与手册同步（文档任务）

> 你是本仓库的资深工程师。本提示词是唯一任务来源：只按本提示词写文档，**不跑任何验收命令（pytest/vitest 一律不跑）**，**不执行任何 git 命令**。
> 红线：禁整文件重写（只做追加式小节 + 最小行内更新）；所有写出的文件必须无 BOM；不得修改 `.env` / `.env.*` / `.env.example` / `.env.production.example`；不得删除任何既有文件或既有段落；不得改动本提示词文件预算之外的任何文件。

## 任务（PRD-V4 联调收尾）

V4 全部 10 个切片已交付并通过验收（切片流水线执行，relay 驱动）。本切片只做**文档同步**：
把 Sprint 10（V4）评审内容写入 TECH_DEBT.md，并在开发提示词手册中补 Sprint 10 入口。
全量回归（后端 pytest + 前端 vitest + build）由审核方亲手执行，**不需要你跑**。

## 已核实的事实（可直接采信，禁止另造数字）

### V4 切片与提交（git log 实核）

| 切片 | 提交 | 内容 | 验收 |
|---|---|---|---|
| V4-1 | c267d97 | F1 修复：同类活动历史 `query_activity_history` + garmin_only 点评注入 | 后端 784→795 |
| V4-2 | a497c63 | F2 修复：exetype 语义（辅助/负重）+ 有效负荷折算 + 历史按 exetype 分组 | 后端 795→816 |
| V4-3 | efa06d3 | F5 后端：skinfold_record 表 + 4 方案公式 + 设置页性别/出生日期 + 测量 API | 后端 816→861 |
| V4-4 | fd98ef0 | F5 前端：设置页表单 + 皮脂钳测量面板 | 前端 289→298 |
| V4-5 | 972a1ff | F3 后端：regenerate_with_feedback 双端点（对话注入反馈段、日限 5 次） | 后端 861→885 |
| V4-6 | 4747334 | F3 前端：详情页内嵌追问 + 重生成按钮 + 海报刷新 | 前端 298→306 |
| V4-7 | 6037b1b | F4 后端：workout_set_hr 表 + set_hr.py + 详情 API set_hr 键 | 后端 885→911 |
| V4-8 | 355a05f | F4 前端：详情页组次表心率列（低置信 ~ 前缀，无数据降级） | 前端 306→312 |
| V4-9 | 3e90ed0 | F4 回填：scripts/backfill_set_hr.py + 逐组心率注入 session_review prompt | 后端 911→918 |

### 最终验收基线（2026-09-02 审核方亲手跑）

- 后端：**918 passed / 6 skipped**（2 PG testcontainers 环境跳过 + 4 显式门禁真实 API 集成测试），
  总覆盖率 **93.21%**（门禁 85% 达标）；基线 784 → 918（+134）。
- 前端：**Vitest 312 passed / 37 文件** + `vite build` 通过；基线 289 → 312（+23）。
- 修订轮次：V4-1/3/4/6/8/9 零修订一轮过；V4-2 fix1 一轮；V4-5 三轮（第 3 轮经人工批准，
  测试断言设计缺陷：SQLite 删行后 id 复用 + 子串碰撞）；V4-7 两轮（均测试自身缺陷：
  e2e 缺 fuse_workout 调用 / 恢复心率夹具同刻撞车）。

### 可登记的新技术债（编号接续 T31 之后）

- **T32**：同 workout 内同名动作共享逐组心率——`workout_set_hr` 按
  (movement_name, set_index) 匹配，同名动作无法区分。影响：真实训记同名动作罕见；
  如需区分，后续给对齐加动作序号维度。
- **T33**：佳明→训记动作类别映射表（`GARMIN_CATEGORY_MAP`）仅 18 类；未识别类别跳过
  校验按高置信处理。遇到佳明识别新类别时补充映射表。
- 保留项维持原判：T23（截图入库服务端幂等）/ T24（PDF echarts 块不渲染）/
  T25（DeepSeek 峰谷定价待校准）/ T30（有氧海报空白）。

### 可登记的经验（Sprint 10 经验登记）

1. 佳明 `exerciseSets.startTime` 为 **GMT 时基** ISO 字符串，与 directTimestamp（epoch ms GMT）
   同基，比较时**严禁 +8 小时**；`activityDetailMetrics` 的 metrics 值是 float，HR 可为 null。
2. 既有 `extract_heart_rate_series` 返回的 `t` 是**数组序号**不是时间戳——需要时间序列时
   不能复用（V4-7 另写 extract_hr_timeline）。
3. 训记组次**无独立时间戳**（time=组间休息秒数），逐组匹配只能以佳明 ACTIVE 组为时间锚、
   训记按全局顺序对齐（done=False 跳过但保留原位置序号）。
4. 测试夹具勿在同一时间刻注入两个不同 HR 点——并列距离使「最近点」断言不确定
   （V4-7-fix2 撞车教训）。
5. SQLite 删行后 id 复用：断言旧 id 为 None 会误判；消息断言防子串碰撞（"消息1" vs
   "消息11"）（V4-5-fix3 教训）。

## 文件预算（共 2 个，不得越界；对应清单「改文档 1-2」）

1. 改 `docs/TECH_DEBT.md`
2. 改 `开发提示词手册.md`

## 开发内容

### 1. `docs/TECH_DEBT.md`

- 头部引言区（第 3-10 行的 `> Sprint N …` 列表）追加一行：
  `> Sprint 10（V4-1~V4-10 点评修正与深度分析增强：同类活动历史 / exetype 语义 / 皮脂钳 / 对话重生成 / 逐组心率）复盘 · 2026-09-02（见第十二节）`
- 文件末尾追加「## 十二、Sprint 10（V4-1 ～ V4-10）评审与复盘 · 2026-09-02」小节，
  沿用既有 Sprint 节格式，含以下子节：
  - **12.1 测试与覆盖率**：最终基线（后端 918+6 / 93.21%，前端 312 / 37 文件 / build 通过）；
    基线增长（784→918、289→312）；说明 V4 采用切片流水线执行（每片审核方亲手重跑验收）。
  - **12.2 十个切片完成度逐项**：上表「V4 切片与提交」照录（切片/提交/内容/验收），
    修订轮次说明照录（含 V4-5 第 3 轮经人工批准、V4-7 两轮均测试自身缺陷）。
  - **12.3 新增技术债**：T32 / T33 照录上表（编号/事项/来源/影响/建议处理 五列沿用既有格式）。
  - **12.4 保留项**：T23/T24/T25/T30 维持原判照录。
  - **12.5 经验登记**：上述 5 条照录。
  - **评审结论**：V4 十片全部完成、验收基线逐项一致无回归、覆盖率门禁维持；
    **建议打 tag `sprint-10`（留待人工执行，流水线不代打 tag）**。

### 2. `开发提示词手册.md`

在「## 附：敏捷纪律速查」一节**之前**插入一个新小节（不改动其他任何既有段落）：

```
## Sprint 10：点评修正与深度分析增强（V4-1 ～ V4-10，2026-09-02）

V4 未采用单条提示词逐任务模式，改用**切片流水线**执行（relay 状态机驱动，每片走
起草→执行→轮询→亲手审核→修订→提交 六阶段）。本节只做索引，不附完整提示词：

- 需求定稿：《PRD-V4.md》（F1 同类活动历史修复 / F2 exetype 语义修正 /
  F3 详情页追评与对话重生成 / F4 逐组心率匹配 / F5 皮脂钳体脂率测量）；
- 切片定义与验收基线：`docs/cursor-prompts/V4-切片清单.md`；
- 各切片提示词：`docs/cursor-prompts/V4-1.md` ～ `V4-10.md`（含 fix 修订稿）；
- 跨轮状态：`logs/v4-relay.json` + `logs/V4-*.state.md`；
- 评审复盘：docs/TECH_DEBT.md 第十二节（测试基线 后端 918+6 / 93.21%、前端 312 / 37 文件）。
```

## 自报告要求（交付时必须给出）

1. 改动文件清单（对照预算 2 个）与各文件改动位置（行号区间）。
2. 确认：TECH_DEBT.md 既有十一节与手册既有内容**零改动**（仅追加）；未跑任何测试命令；
   未执行 git；文件无 BOM；未碰 .env 系列；数字与本提示词给定值完全一致（未自造）。
